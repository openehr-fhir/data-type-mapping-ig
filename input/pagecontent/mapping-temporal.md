### Temporal data

openEHR's temporal values inherit from the abstract `DV_TEMPORAL` class:
`DV_DATE`, `DV_TIME`, `DV_DATE_TIME`, and `DV_DURATION`. FHIR uses the `date`,
`dateTime`, `instant`, and `time` primitives, plus `Duration` (a constrained
`Quantity`) and `Period`.

Both systems use ISO 8601, which makes this category look easier than it is.
Six facts govern the rows.

**The two ISO 8601 subsets are not the same subset.** openEHR permits compact
forms — `20250301`, `T143000` — while FHIR requires the extended form —
`2025-03-01`, `14:30:00`. Compact values SHALL be expanded before mapping to
FHIR. The precise comparison is published as a machine-generated table on
[Cross-Cutting Concerns](cross-cutting.html), rendered directly from the code
that implements the conversion, so the guide and the reference implementation
cannot disagree about it.

**Partial precision is meaningful and SHALL be preserved.** Where an openEHR
value is partial — year and month only — the FHIR lexical form is **truncated**
to match, never padded. `202604` becomes `2026-04`, not `2026-04-01`. Padding
invents a day that the source did not record.

That rule holds for every partial form the FHIR regexes admit, which is every
`date` and every `dateTime` down to the year. It **cannot** hold in one place:
FHIR `time` makes seconds mandatory, and FHIR `dateTime` makes them mandatory
once a time is present, so an openEHR `14:30` or `2026-03-01T14:30` has no
shorter FHIR form to truncate to. Those two values SHALL be completed to
`14:30:00` and `2026-03-01T14:30:00`, and the completion is a **named loss** —
`DV_TIME.value[minute-precision]` and `DV_DATE_TIME.value[minute-precision]` —
not a silent one. FHIR additionally requires a UTC offset alongside a time in a
`dateTime`: that offset SHALL come from the source or its context, and a
data-type conversion never invents one.

**Fractional seconds differ in width.** FHIR allows up to nine decimal digits;
openEHR restricts to three. Excess precision is truncated when mapping FHIR →
openEHR, which is a named drop rather than a rounding decision.

**FHIR `time` cannot carry a time zone.** `dateTime` and `instant` can. Where an
openEHR `DV_TIME` carries a time zone, the FHIR `timezone` extension is used.

**Some FHIR resources split a date and its time across two elements.**
`Patient.birthDate` is a `date`, with the time conveyed by the
`patient-birthTime` extension. A mapping engine has to know these
element-specific patterns; they are not derivable from the data type alone.

**Duration needs a real conversion.** ISO 8601 durations and UCUM quantities are
different representations, and not every ISO 8601 duration has a UCUM
equivalent. The conversion is implemented in the
[reference implementation](reference-implementation.html) and the
non-convertible cases are reported rather than silently approximated.

Date and time intervals map to `Period` and are covered here. Numeric intervals
map to `Range` and are covered in [Quantities](mapping-quantity.html).

See [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:temporal (generated by npm --prefix reference run render) >>> -->
| openEHR type | FHIR type | → FHIR | → openEHR | Maturity | Scope |
|-|-|-|-|-|-|
| `DV_DATE` | `date \| dateTime` | `lossless` | `lossless` | `settled` | `datatype` |
| `DV_TIME` | `time` | `lossy` | `lossy` | `open` | `datatype` |
| `DV_DATE_TIME` | `dateTime \| instant` | `lossy` | `lossy` | `settled` | `datatype` |
| `DV_DURATION` | `Duration` | `lossy` | `lossy` | `open` | `datatype` |
| `DV_GENERAL_TIME_SPECIFICATION / DV_PERIODIC_TIME_SPECIFICATION` | `Timing` | `unmapped` | `unmapped` | `not-discussed` | `datatype` |
<!-- <<< mapping ledger: summary:temporal (generated by npm --prefix reference run render) <<< -->

### DV_DATE and date / dateTime

<!-- >>> mapping ledger: mapping:dv-date-to-date (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_DATE](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_date_class) · [openEHR BASE — ISO 8601 time types](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_date_time_package) · [FHIR R5 — date](https://hl7.org/fhir/R5/datatypes.html#date)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_DATE.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_date_class) `1..1` | *when* the target element is typed `date`: [date](https://hl7.org/fhir/R5/datatypes.html#date) <br/>*when* the target element is typed `dateTime` and carries only a date: [dateTime](https://hl7.org/fhir/R5/datatypes.html#dateTime) | `lossless` | `lossless` | `settled` | openEHR permits **compact** forms (`20260301`, `202603`) and FHIR requires the extended form, so a compact value SHALL be expanded before mapping. Partial precision is preserved by **truncating** the FHIR lexical form, never padding it: `202604` becomes `2026-04`, not `2026-04-01`. Some FHIR resources split a date from its time — `Patient.birthDate` is a `date` with the time in the `patient-birthTime` extension — and a mapping engine has to know those patterns, because they are not derivable from the data type. |
<!-- <<< mapping ledger: mapping:dv-date-to-date (generated by npm --prefix reference run render) <<< -->

#### Worked example - a partial date

Year-and-month precision is preserved by truncating the FHIR lexical form, never by
padding it.

<!-- >>> mapping ledger: example:dv-date-to-date/02-partial-date.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_DATE",
  "value": "2026-04"
}
```
<!-- <<< mapping ledger: example:dv-date-to-date/02-partial-date.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-date-to-date/02-partial-date.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
"2026-04"
```
<!-- <<< mapping ledger: example:dv-date-to-date/02-partial-date.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_TIME and time

<!-- >>> mapping ledger: mapping:dv-time-to-time (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_TIME](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_time_class) · [openEHR BASE — ISO 8601 time types](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_date_time_package) · [FHIR R5 — time](https://hl7.org/fhir/R5/datatypes.html#time)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_TIME.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_time_class) `1..1` | [time.value](https://hl7.org/fhir/R5/datatypes.html#time) | `lossless` | `lossy` | `settled` | → openEHR: drops `time.value` — FHIR permits up to **nine** fractional-second digits and openEHR restricts to **three**, so anything finer than a millisecond is truncated FHIR `time` also requires seconds, so an openEHR `14:30` is completed to `14:30:00`. That sub-case is a **named drop** in its own right and is stated on the row below, not folded into this one. |
| [DV_TIME.value[minute-precision]](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_time_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#time)) | `lossy` | `unmapped` | `settled` | → FHIR: drops `DV_TIME.value[minute-precision]` — an openEHR `14:30` is completed to `14:30:00`, so the FHIR value states a precision the source did not. This is the one case the guide’s "truncate, never pad" rule cannot cover, because FHIR offers no shorter `time` form to truncate to → openEHR: Nothing arrives to map back: a completed `14:30:00` is indistinguishable from one the source stated in full. The sub-case is split out so the round-trip matrix can police it: the completion is reported at this path, and the parent `DV_TIME.value` row keeps its `lossless` claim for every value that already states seconds. |
| [DV_TIME.value[timezone]](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_time_class) `0..1` | [time.extension[timezone]](https://hl7.org/fhir/extensions/StructureDefinition-timezone.html)<sup>†</sup> *(extension)* | `lossless` | `lossless` | `open` | **FHIR `time` cannot carry a time zone**, while `dateTime` and `instant` can. Where an openEHR `DV_TIME` carries an offset it is moved into the `timezone` extension on the element and restored from there. openEHR data without a time zone is rare in practice. |
<!-- <<< mapping ledger: mapping:dv-time-to-time (generated by npm --prefix reference run render) <<< -->

#### Worked example - a time with an offset

<!-- >>> mapping ledger: example:dv-time-to-time/01-collection-time.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_TIME",
  "value": "14:30:00+01:00"
}
```
<!-- <<< mapping ledger: example:dv-time-to-time/01-collection-time.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-time-to-time/01-collection-time.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "value": "14:30:00",
  "extension": [
    {
      "url": "http://hl7.org/fhir/StructureDefinition/timezone",
      "valueString": "+01:00"
    }
  ]
}
```
<!-- <<< mapping ledger: example:dv-time-to-time/01-collection-time.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_DATE_TIME and dateTime / instant

<!-- >>> mapping ledger: mapping:dv-date-time-to-date-time (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_DATE_TIME](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_date_time_class) · [openEHR RM — DV_TEMPORAL](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_temporal_class) · [FHIR R5 — dateTime](https://hl7.org/fhir/R5/datatypes.html#dateTime) · [FHIR R5 — instant](https://hl7.org/fhir/R5/datatypes.html#instant)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_DATE_TIME.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_date_time_class) `1..1` | *when* the target element is typed `dateTime`: [dateTime](https://hl7.org/fhir/R5/datatypes.html#dateTime) <br/>*when* the value is fully precise and the target element is typed `instant`: [instant](https://hl7.org/fhir/R5/datatypes.html#instant) | `lossless` | `lossless` | `settled` | Unlike `time`, `dateTime` carries a UTC offset directly, and its R5 regex admits partial precision down to the year, so `2026-03` is **truncated**, never padded. What the regex does *not* admit is a time without seconds; that sub-case is a named drop on the row below. |
| [DV_DATE_TIME.value[minute-precision]](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_date_time_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#dateTime)) | `lossy` | `unmapped` | `settled` | → FHIR: drops `DV_DATE_TIME.value[minute-precision]` — an openEHR `2026-03-01T14:30` is completed to `2026-03-01T14:30:00`, so the FHIR value states a precision the source did not. FHIR additionally requires a UTC offset alongside a time: that offset SHALL come from the source or its context and is **never invented** by a data-type conversion → openEHR: Nothing arrives to map back: a completed `…T14:30:00` is indistinguishable from one the source stated in full. Partial precision *above* the time — `2026`, `2026-03`, `2026-03-01` — is a different case entirely and is carried by truncation, losslessly, on the row above. Only the minute-without-seconds form has no FHIR representation. |
| [DV_DATE_TIME.accuracy](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_temporal_class) `0..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#dateTime)) | `unmapped` | `unmapped` | `settled` | → FHIR: Dropped. `accuracy` and `magnitude_status`, inherited from `DV_TEMPORAL` and `DV_QUANTIFIED`, have no FHIR home on a temporal primitive; where the source value is partial, that precision is preserved by truncating the lexical form instead. → openEHR: Nothing arrives, because nothing was emitted. |
<!-- <<< mapping ledger: mapping:dv-date-time-to-date-time (generated by npm --prefix reference run render) <<< -->

### DV_DURATION and Duration

<!-- >>> mapping ledger: mapping:dv-duration-to-duration (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_DURATION](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_duration_class) · [FHIR R5 — Duration](https://hl7.org/fhir/R5/datatypes.html#Duration)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_DURATION.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_duration_class) `1..1` | [Duration.value](https://hl7.org/fhir/R5/datatypes.html#Duration) | `lossy` | `lossless` | `open` | → FHIR: drops `DV_DURATION.value` — an ISO 8601 duration naming **more than one** component — `P1Y6M` — has no single UCUM unit, because the number of days in a month and in a year is not fixed; combining them would invent a precision the source does not have A UCUM quantity carries one unit, so a single-component duration converts cleanly: `a` ↔ `P{n}Y`, `mo` ↔ `P{n}M`, `wk` ↔ `P{n}W`, `d` ↔ `P{n}D`, `h` ↔ `PT{n}H`, `min` ↔ `PT{n}M`, and `s` ↔ `PT{n}S`. `ms` is the exception: it has no ISO 8601 designator of its own, so `{n}` ms is written as `PT{n/1000}S` — the **value** is preserved exactly and the **unit is not**, because reading that duration back names seconds, not milliseconds. That asymmetry is reported as a drop rather than claimed lossless. Note that `M` means *months* before the `T` separator and *minutes* after it. Even within UCUM, date and time conversions are not straightforward, and sub-millisecond precision may be lost. A standalone conversion library is planned by the working group; if one is published it supersedes the reference helper rather than conflicting with it. |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_duration_class)) | [Duration.code](https://hl7.org/fhir/R5/datatypes.html#Duration) | `unmapped` | `unmapped` | `settled` | → FHIR: No openEHR field produces it; the unit is derived from the ISO 8601 designator, and `Duration.system` is fixed to UCUM. → openEHR: The unit is folded back into the ISO 8601 lexical form rather than carried as a field of its own. |
| [DV_DURATION.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_duration_class) `1..1` | [Timing.repeat](https://hl7.org/fhir/R5/datatypes.html#Timing) | `lossless` | `lossless` | `open` | `archetype` scope. Complex scheduling — dosage and treatment plans — is handled in openEHR by a combination of four archetypes: **daily timing**, **non-daily timing**, **service direction**, and **therapeutic direction**, the last two differing only in whether the use is medication-related. The full timing and dosage mapping is acknowledged as **the hardest mapping problem** and is deferred to a dedicated archetype-level session, not least because both standards are changing: the R6 approach to `Timing` appears closer to the openEHR one. |
<!-- <<< mapping ledger: mapping:dv-duration-to-duration (generated by npm --prefix reference run render) <<< -->

#### Worked example - six months

<!-- >>> mapping ledger: example:dv-duration-to-duration/01-six-months.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_DURATION",
  "value": "P6M"
}
```
<!-- <<< mapping ledger: example:dv-duration-to-duration/01-six-months.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-duration-to-duration/01-six-months.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "value": 6,
  "system": "http://unitsofmeasure.org",
  "code": "mo"
}
```
<!-- <<< mapping ledger: example:dv-duration-to-duration/01-six-months.fhir.json (generated by npm --prefix reference run render) <<< -->

### Time specifications and Timing

<!-- >>> mapping ledger: mapping:dv-time-specification-to-timing (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — Time specification package](https://specifications.openehr.org/releases/RM/latest/data_types.html#_time_specification_package) · [FHIR R5 — Timing](https://hl7.org/fhir/R5/datatypes.html#Timing)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_GENERAL_TIME_SPECIFICATION.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_general_time_specification_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Timing)) | `unmapped` | `unmapped` | `not-discussed` | → FHIR: **Not yet discussed.** No mapping is asserted, because none has been agreed. (owner: `working-group`) → openEHR: **Not yet discussed.** (owner: `working-group`) |
| [DV_PERIODIC_TIME_SPECIFICATION.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_periodic_time_specification_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Timing)) | `unmapped` | `unmapped` | `not-discussed` | → FHIR: **Not yet discussed.** (owner: `working-group`) → openEHR: **Not yet discussed.** (owner: `working-group`) |
<!-- <<< mapping ledger: mapping:dv-time-specification-to-timing (generated by npm --prefix reference run render) <<< -->
