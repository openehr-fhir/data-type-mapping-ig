### Other data

This category holds the types that do not fit any of the others: encapsulated
binary and parsable content, and state machines.

**`DV_MULTIMEDIA` ↔ `Attachment`** is the substantial mapping here. The two
types agree on the essentials — media type, data, size, an integrity hash, and a
URI alternative to inline data — and diverge on almost everything around them.
`DV_MULTIMEDIA` carries a compression algorithm, an integrity check algorithm
that need not be SHA-1, and a thumbnail; `Attachment` carries a creation
timestamp, height, width, frames, duration, pages, and a language. Neither set
has a home on the other side, and each of those is recorded as an `unmapped`
row with the ticket that owns it. **No extension, code system, or value set is
invented here for any of them.**

**`DV_PARSABLE`** wraps a string with a `formalism` naming the syntax it is
written in. The value maps to a FHIR `string`, or to `markdown` where the
formalism is markdown. **The `formalism` itself has no FHIR home.** The
`mimeType` extension is not it: its declared context is `Questionnaire.item`
and `ElementDefinition`, not `string` or any data type, and its purpose is a
design-time constraint on the attachments an element permits rather than a
statement about an instance. No substitute is invented here, so the gap is
published and the mapping is **one-directional**: a `DV_PARSABLE` becomes a
`string`, and a `string` alone cannot become a `DV_PARSABLE`, because
`formalism` is mandatory and only the element definition knows it. The decision
is [open](open-items.html).

**`DV_STATE`** carries a coded value plus an `is_terminal` flag and belongs to
an instruction state machine. FHIR has no data type for it: FHIR models workflow
state as resource-level elements such as `Task.status`, with its own state
machine. The value can be carried as a `CodeableConcept`, but the state machine
it belongs to cannot, so this is largely `unmapped` and the reason is recorded
rather than papered over.

`DV_ENCAPSULATED`, the abstract parent of `DV_MULTIMEDIA` and `DV_PARSABLE`, is
also noted here. The openEHR Reference Model is the authority for its
definition, and this guide cites the RM rather than any downstream
representation of it.

Reviewer coverage in this category is uneven — several of these types were never
reviewed from the FHIR side. That is published in full on
[Open Items](open-items.html) rather than smoothed over, and rows the
specifications do not clearly support are recorded `open` rather than asserted.

See [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:other (generated by npm --prefix reference run render) >>> -->
| openEHR type | FHIR type | → FHIR | → openEHR | Maturity | Scope |
|-|-|-|-|-|-|
| `DV_MULTIMEDIA` | `Attachment` | `lossy` | `lossy` | `open` | `datatype` |
| `DV_PARSABLE` | `string` | `lossy` | `unmapped` | `open` | `datatype` |
| `DV_STATE` | `CodeableConcept` | `lossy` | `lossy` | `open` | `datatype` |
<!-- <<< mapping ledger: summary:other (generated by npm --prefix reference run render) <<< -->

### DV_MULTIMEDIA and Attachment

<!-- >>> mapping ledger: mapping:dv-multimedia-to-attachment (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_MULTIMEDIA](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) · [FHIR R5 — Attachment](https://hl7.org/fhir/R5/datatypes.html#Attachment) · [openEHR RM — DV_ENCAPSULATED](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_encapsulated_class)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_MULTIMEDIA.data](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `0..1` | [Attachment.data](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `lossless` | `lossless` | `open` |  |
| [DV_MULTIMEDIA.uri](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `0..1` | [Attachment.url](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `lossless` | `lossless` | `open` |  |
| [DV_MULTIMEDIA.media_type](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `1..1` | [Attachment.contentType](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `lossless` | `lossless` | `open` | An IANA MIME type on both sides. Where an openEHR `charset` applies to the attachment data itself it SHOULD be folded into the MIME type as a `charset` parameter; where it applies to local content the content is converted to UTF-8. The openEHR attribute is **mandatory** and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces **nothing** rather than inventing a value. See [the mandatory-attribute rule](conventions.html#mandatory-attributes). |
| [DV_MULTIMEDIA.size](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `1..1` | [Attachment.size](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `lossless` | `lossless` | `open` | The **original** size in bytes, before any compression or encoding. `0` is a real size, not a missing one. The two types are **not the same width**: RM `Integer` is 32-bit and R5 `Attachment.size` is an `integer64`, so an attachment larger than 2,147,483,647 bytes has no `DV_MULTIMEDIA.size` to land in. That sub-case is the row below, and it is the same gap the numeric category publishes as `Integer64[overflow]` on [Numeric Primitives](mapping-numeric.html). The openEHR attribute is **mandatory** and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces **nothing** rather than inventing a value. See [the mandatory-attribute rule](conventions.html#mandatory-attributes). |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class)) | [Attachment.size[overflow]](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `unmapped` | `unmapped` | `open` | → FHIR: openEHR cannot produce a value outside its own 32-bit range, so this case never arises in this direction. (owner: `openehr-modelling`) → openEHR: Nothing in openEHR can receive it, and `size` is mandatory, so no `DV_MULTIMEDIA` is produced at all rather than one carrying a truncated or invented size. Widening `DV_MULTIMEDIA.size` is an openEHR Reference Model question. (owner: `openehr-modelling`) Cross-refer to `Integer64[overflow]` on [Numeric Primitives](mapping-numeric.html): it is the identical 32-bit/64-bit gap, and publishing one as a first-class `unmapped` row while folding the other into a `lossless` claim was a transcription slip rather than a position. |
| [DV_MULTIMEDIA.alternate_text](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `0..1` | [Attachment.title](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `lossless` | `lossless` | `open` | Display text to show in place of the multimedia content. |
| [DV_MULTIMEDIA.integrity_check](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `0..1` | [Attachment.hash](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `lossless` | `lossless` | `open` | `Attachment.hash` is **defined as SHA-1**. A hash computed with any other algorithm is carried only alongside `integrity_check_algorithm` — see the next row. |
| [DV_MULTIMEDIA.integrity_check_algorithm](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `0..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Attachment)) | `unmapped` | `unmapped` | `open` | → FHIR: openEHR permits SHA-1, SHA-224, SHA-256, SHA-384, SHA-512, SHA-512/224, and SHA-512/256; only SHA-1 has a home in R5. FHIR-55422 is **resolved** and adds a core `alternate-hash` extension keeping SHA-1 as the default, but it has not been published yet. **No extension is invented here.** (owner: [FHIR-55422](https://jira.hl7.org/browse/FHIR-55422)) → openEHR: Nothing arrives; a receiving system MAY assume SHA-1, which is an inference from the FHIR definition rather than a carried value. (owner: [FHIR-55422](https://jira.hl7.org/browse/FHIR-55422)) |
| [DV_MULTIMEDIA.compression_algorithm](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `0..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Attachment)) | `unmapped` | `unmapped` | `open` | → FHIR: openEHR permits `compress`, `deflate`, `gzip`, `zlib`, and `other`; FHIR has no equivalent. FHIR-56003 requests a `content-encoding` extension and is triaged. The pragmatic alternative is to **decompress during conversion**, which removes the need for the field. **No extension is invented here.** (owner: [FHIR-56003](https://jira.hl7.org/browse/FHIR-56003)) → openEHR: Nothing arrives, because nothing was emitted. (owner: [FHIR-56003](https://jira.hl7.org/browse/FHIR-56003)) |
| [DV_MULTIMEDIA.thumbnail](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class) `0..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Attachment)) | `unmapped` | `unmapped` | `open` | → FHIR: A nested thumbnail has no FHIR home. A new extension on `Attachment` has been requested as FHIR-56002 and is **not yet published**. **No extension is invented here.** (owner: [FHIR-56002](https://jira.hl7.org/browse/FHIR-56002)) → openEHR: Nothing arrives, because nothing was emitted. (owner: [FHIR-56002](https://jira.hl7.org/browse/FHIR-56002)) |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_multimedia_class)) | [Attachment.creation](https://hl7.org/fhir/R5/datatypes.html#Attachment) | `unmapped` | `unmapped` | `open` | → FHIR: No `DV_MULTIMEDIA` field produces any of them. (owner: `openehr-modelling`) → openEHR: Carried by an **extension archetype**, not by the data type. This guide links the archetype and describes it; authoring it belongs to the openEHR modelling team, who have an open proposal to fold the extended elements into the published *Media File* CLUSTER. (owner: `openehr-modelling`) It applies equally to `Attachment.height`, `.width`, `.frames`, `.duration`, `.pages`, and `.language`. The *Media File* CLUSTER archetype, rather than a bare `DV_MULTIMEDIA`, is the closer target for a FHIR `Attachment`. The row is `datatype` scope because its FHIR side is an ordinary data-type element, not a resource element: the conversion is what has nowhere to put it, and the round-trip matrix now says so. |
| [DV_MULTIMEDIA.charset](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_encapsulated_class) `0..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Attachment)) | `unmapped` | `unmapped` | `settled` | → FHIR: Not carried as a field. Where the charset applies to the attachment data it is folded into `contentType`; where it applies to local content the content is converted to UTF-8. → openEHR: Nothing arrives as a distinct field. The `DV_ENCAPSULATED` parent also brings `language`. Note that the openEHR **Base IG** models `DV_ENCAPSULATED` differently from the Reference Model; **the Reference Model is the authority** cited here. |
<!-- <<< mapping ledger: mapping:dv-multimedia-to-attachment (generated by npm --prefix reference run render) <<< -->

#### Worked example - a referenced image

<!-- >>> mapping ledger: example:dv-multimedia-to-attachment/01-chest-xray.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_MULTIMEDIA",
  "media_type": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "IANA_media-types"
    },
    "code_string": "image/png"
  },
  "size": 20416,
  "uri": {
    "_type": "DV_URI",
    "value": "https://example.org/images/chest-xray.png"
  },
  "alternate_text": "Chest X-ray, PA view"
}
```
<!-- <<< mapping ledger: example:dv-multimedia-to-attachment/01-chest-xray.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-multimedia-to-attachment/01-chest-xray.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "contentType": "image/png",
  "url": "https://example.org/images/chest-xray.png",
  "size": 20416,
  "title": "Chest X-ray, PA view"
}
```
<!-- <<< mapping ledger: example:dv-multimedia-to-attachment/01-chest-xray.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_PARSABLE and string

<!-- >>> mapping ledger: mapping:dv-parsable-to-string (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_PARSABLE](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_parsable_class) · [FHIR R5 — string](https://hl7.org/fhir/R5/datatypes.html#string) · [openEHR RM — DV_ENCAPSULATED](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_encapsulated_class)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_PARSABLE.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_parsable_class) `1..1` | [string.value](https://hl7.org/fhir/R5/datatypes.html#string) | `lossless` | `unmapped` | `open` | → openEHR: A FHIR `string` states nothing about the syntax its value is written in, and `DV_PARSABLE.formalism` is mandatory, so **no `DV_PARSABLE` can be produced from a `string` alone**. The formalism has to come from the element definition, which a data-type conversion never sees. This mapping is one-directional. (owner: `session:dv-parsable-formalism`) Where the formalism is markdown, a `markdown` element is the better target. For genomic content such as HGVS, the Genomics Reporting IG uses a `CodeableConcept.text` pattern instead. |
| [DV_PARSABLE.formalism](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_parsable_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#string)) | `unmapped` | `unmapped` | `open` | → FHIR: Nothing carries it. The working group recorded a **Decision made** to use the `mimeType` extension; the extension definition does not support that decision, and this guide publishes the gap rather than an instance no validator accepts. (owner: `session:dv-parsable-formalism`) → openEHR: Nothing arrives to map back, because nothing was emitted. (owner: `session:dv-parsable-formalism`) `formalism` is **mandatory** in openEHR, so this gap is what makes `DV_PARSABLE ↔ string` a one-way mapping: the value travels to FHIR, and nothing travels back. Inventing a FHIR usage to fill an openEHR gap is exactly what this guide will not do, so no substitute extension is proposed here — the decision belongs to the working group, and it is recorded as open. |
<!-- <<< mapping ledger: mapping:dv-parsable-to-string (generated by npm --prefix reference run render) <<< -->

#### Worked example - an HGVS expression

<!-- >>> mapping ledger: example:dv-parsable-to-string/01-hgvs.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_PARSABLE",
  "value": "NM_000059.3:c.274G>A",
  "formalism": "text/x-hgvs"
}
```
<!-- <<< mapping ledger: example:dv-parsable-to-string/01-hgvs.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-parsable-to-string/01-hgvs.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "value": "NM_000059.3:c.274G>A"
}
```
<!-- <<< mapping ledger: example:dv-parsable-to-string/01-hgvs.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_STATE and CodeableConcept

<!-- >>> mapping ledger: mapping:dv-state-to-codeable-concept (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_STATE](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_state_class) · [FHIR R5 — CodeableConcept](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_STATE.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_state_class) `1..1` | [CodeableConcept](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept) | `lossless` | `lossless` | `open` | The state **name** maps as an ordinary `DV_CODED_TEXT`; see [Coded Data](mapping-coded.html). What does **not** map is the state machine it belongs to. The openEHR attribute is **mandatory** and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces **nothing** rather than inventing a value. See [the mandatory-attribute rule](conventions.html#mandatory-attributes). |
| [DV_STATE.is_terminal](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_state_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept)) | `unmapped` | `unmapped` | `open` | → FHIR: There is no FHIR element for it and **no extension is invented here**. Carrying it would require either an extension or handling at the archetype and resource level. (owner: `working-group`) → openEHR: `is_terminal` is mandatory in openEHR, so a mapping engine must infer it from the state machine the archetype defines — an inference, not a carried value. (owner: `working-group`) `DV_STATE` sees very little use. One participant has never seen it used at all and suggests it is legacy, superseded by `ISM_TRANSITION`. This row is one of the two **recorded exceptions** to [the mandatory-attribute rule](conventions.html#mandatory-attributes): nothing in a `CodeableConcept` can source `is_terminal`, the openEHR-only gap is published here, and the reference implementation infers `false` rather than refusing the whole conversion. `contract.test.ts` pins the exception, so the list cannot grow unnoticed. |
<!-- <<< mapping ledger: mapping:dv-state-to-codeable-concept (generated by npm --prefix reference run render) <<< -->
