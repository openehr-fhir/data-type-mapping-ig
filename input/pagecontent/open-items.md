### Open items and review coverage

This guide is a work in progress, and it says so in detail rather than in the
abstract. This page publishes two things the working group needs and readers
deserve:

- **Reviewer coverage** — which mappings have been reviewed from the openEHR
  side, from the FHIR side, from both, or from neither. Coverage is uneven. A
  mapping reviewed on one side only carries more risk of an unsupported claim
  than one reviewed on both, and hiding that would misrepresent the guide's
  maturity.
- **The open-items register** — every unresolved action, who owns it, and what
  its current status is.

Open items fall into three groups.

**FHIR-side actions** are HL7 Jira tickets against the FHIR specification
itself: changes to `Quantity.comparator`, to `Attachment`, and to the
terminology guidance this guide depends on. This guide **references** their
status; it does not advance them, and it does not publish an R6-era item as
though it were R5 content.

**openEHR-side actions** are changes to the openEHR Reference Model or to its
published terminology, owned by the openEHR modelling team: binding strengths,
`code_string` constraints, `TERM_MAPPING.purpose`, and the pending
`LINK` / `PARTY_IDENTIFIED` / `OBJECT_REF` unification.

**Documentation and tooling actions** are this guide's own backlog — the
concept-map pass, the archetypes that several gap rows depend on, and the
questions the working group has not yet decided.

Every ticket cited anywhere in a mapping row appears in the register below.
That is enforced mechanically, so a gap cannot cite an owner that this page then
fails to list.

An item's presence here is not an admission of a defect. It is the guide
declining to assert something nobody has agreed.

See [Gaps](gaps.html) for the mapping-level consequences of these items, and
[Conventions](conventions.html) for what `open` and `not-discussed` mean.

### Reviewer coverage

Which mappings have been reviewed, and from which side. **unchecked** means exactly
that: nobody has reviewed the mapping from that side, and a claim in it carries more
risk than one that has been checked twice.

<!-- >>> mapping ledger: review-coverage (generated by npm --prefix reference run render) >>> -->

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid">
<thead>
<tr><th>Mapping</th><th>Category</th><th>openEHR review</th><th>FHIR review</th></tr>
</thead>
<tbody>
<tr><td><a href="mapping-quantity.html#mapping-dv-quantity-to-quantity">DV_QUANTITY ↔ Quantity</a></td><td>Quantities</td><td>✓ Diego, Ian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-quantity.html#mapping-dv-count-to-count">DV_COUNT ↔ Count</a></td><td>Quantities</td><td>✓ Diego, Ian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-quantity.html#mapping-dv-proportion-to-ratio">DV_PROPORTION ↔ Ratio</a></td><td>Quantities</td><td>✓ Diego, Ian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-quantity.html#mapping-dv-interval-to-range">DV_INTERVAL ↔ Range / Period / Quantity</a></td><td>Quantities</td><td>✓ Diego, Ian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-quantity.html#mapping-dv-quantity-to-money">DV_QUANTITY ↔ Money / MoneyQuantity</a></td><td>Quantities</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-quantity.html#mapping-dv-quantity-to-simple-quantity">DV_QUANTITY ↔ SimpleQuantity</a></td><td>Quantities</td><td>✓ Diego, Ian</td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-quantity.html#mapping-dv-ordinal-to-observation-component">DV_ORDINAL ↔ Observation.component</a></td><td>Quantities</td><td>✓ Diego, Ian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-quantity.html#mapping-dv-scale-to-observation-component">DV_SCALE ↔ Observation.component</a></td><td>Quantities</td><td>✓ Diego, Ian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-coded.html#mapping-code-phrase-to-coding">CODE_PHRASE ↔ Coding</a></td><td>Coded Data</td><td>✓ Severin</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-coded.html#mapping-dv-coded-text-to-codeable-concept">DV_CODED_TEXT ↔ CodeableConcept / Coding</a></td><td>Coded Data</td><td>✓ Severin</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-coded.html#mapping-term-mapping-to-coding">TERM_MAPPING ↔ CodeableConcept.coding</a></td><td>Coded Data</td><td>✓ Severin</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-coded.html#mapping-null-flavour-to-data-absent-reason">null_flavour ↔ data-absent-reason</a></td><td>Coded Data</td><td>✓ Severin</td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-boolean.html#mapping-dv-boolean-to-boolean">DV_BOOLEAN ↔ boolean</a></td><td>Boolean Data</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-numeric.html#mapping-integer-to-integer">Integer ↔ integer</a></td><td>Numeric Primitives</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-numeric.html#mapping-integer64-to-integer64">Integer64 ↔ integer64</a></td><td>Numeric Primitives</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-numeric.html#mapping-real-to-decimal">Real / Double ↔ decimal</a></td><td>Numeric Primitives</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-textual.html#mapping-dv-text-to-string">DV_TEXT ↔ string / markdown</a></td><td>Textual Data</td><td>✓ Ciprian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-textual.html#mapping-dv-paragraph-to-markdown">DV_PARAGRAPH ↔ markdown / string</a></td><td>Textual Data</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-reference.html#mapping-dv-identifier-to-identifier">DV_IDENTIFIER ↔ Identifier</a></td><td>Resource-Locator Data and References</td><td>✓ Severin</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-reference.html#mapping-dv-uri-to-uri">DV_URI / DV_EHR_URI ↔ uri / url</a></td><td>Resource-Locator Data and References</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-reference.html#mapping-link-to-reference">LINK ↔ Reference / CodeableReference</a></td><td>Resource-Locator Data and References</td><td>✓ Severin</td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-temporal.html#mapping-dv-date-to-date">DV_DATE ↔ date / dateTime</a></td><td>Temporal Data</td><td>✓ Ciprian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-temporal.html#mapping-dv-time-to-time">DV_TIME ↔ time</a></td><td>Temporal Data</td><td>✓ Ciprian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-temporal.html#mapping-dv-date-time-to-date-time">DV_DATE_TIME ↔ dateTime / instant</a></td><td>Temporal Data</td><td>✓ Ciprian</td><td>✓ Gino</td></tr>
<tr><td><a href="mapping-temporal.html#mapping-dv-duration-to-duration">DV_DURATION ↔ Duration</a></td><td>Temporal Data</td><td>✓ Diego, Ian, Ciprian</td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-temporal.html#mapping-dv-time-specification-to-timing">Time specifications ↔ Timing</a></td><td>Temporal Data</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-other.html#mapping-dv-multimedia-to-attachment">DV_MULTIMEDIA ↔ Attachment</a></td><td>Other Data</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-other.html#mapping-dv-parsable-to-string">DV_PARSABLE ↔ string</a></td><td>Other Data</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="mapping-other.html#mapping-dv-state-to-codeable-concept">DV_STATE ↔ CodeableConcept</a></td><td>Other Data</td><td>✓ Severin</td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="gaps.html">FHIR features with no openEHR counterpart</a></td><td>Gaps</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
<tr><td><a href="gaps.html">openEHR constructs not yet discussed</a></td><td>Gaps</td><td><strong>unchecked</strong></td><td><strong>unchecked</strong></td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: review-coverage (generated by npm --prefix reference run render) <<< -->

### The register

Every ticket cited anywhere in a mapping row appears below. That is enforced
mechanically, so a gap cannot blame an owner this page then fails to list.

<!-- >>> mapping ledger: open-items (generated by npm --prefix reference run render) >>> -->

#### FHIR-side actions

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid">
<thead>
<tr><th>Item</th><th>Owner</th><th>Priority</th><th>Status</th></tr>
</thead>
<tbody>
<tr><td>Add <code>defining_code</code> to the <code>coding-purpose</code> code system<br/><a href="https://jira.hl7.org/browse/HTA-170">HTA-170</a></td><td>Brett Esler</td><td><code>high</code></td><td>Open. Submitted to HL7 Terminology. The group is trending towards the pragmatic position that <code>userSelected</code> and <code>defining_code</code> are equivalent enough, and may withdraw the request.</td></tr>
<tr><td>Add <code>~</code> (approximate) to <code>Quantity.comparator</code><br/><a href="https://jira.hl7.org/browse/FHIR-56000">FHIR-56000</a></td><td>Gino</td><td><code>medium</code></td><td>Resolved, not applied. Lands in R6 with guidance on stating an accuracy range and a default assumption of ±10%. Not available in an R5 instance.</td></tr>
<tr><td>A hash-algorithm extension on <code>Attachment</code><br/><a href="https://jira.hl7.org/browse/FHIR-55422">FHIR-55422</a></td><td>Gino</td><td><code>medium</code></td><td>Resolved, change required. HL7 agreed to define a core <code>alternate-hash</code> extension while keeping SHA-1 as the default. Not yet published.</td></tr>
<tr><td>A compression-algorithm extension on <code>Attachment</code><br/><a href="https://jira.hl7.org/browse/FHIR-56003">FHIR-56003</a></td><td>Gino</td><td><code>low</code></td><td>Triaged. The pragmatic alternative is to decompress during conversion, which removes the need for the field entirely.</td></tr>
<tr><td>A FHIR representation for <code>pk_fraction</code> and <code>pk_integer_fraction</code> formatting<br/><a href="https://jira.hl7.org/browse/FHIR-56001">FHIR-56001</a></td><td>Gino</td><td><code>medium</code></td><td>Triaged. These are display directives; a search of <code>Ratio</code> found nothing suitable, and the desire is to use the standard <code>rendered-value</code> extension.</td></tr>
<tr><td>A thumbnail extension on <code>Attachment</code><br/><a href="https://jira.hl7.org/browse/FHIR-56002">FHIR-56002</a></td><td>Gino</td><td><code>medium</code></td><td>Triaged. The existing <code>documentreference-thumbnail</code> extension is a flag on <code>DocumentReference.content</code> saying the content <em>is</em> a thumbnail; a new extension on <code>Attachment</code> was requested instead.</td></tr>
</tbody>
</table>
</div>

#### openEHR-side actions

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid">
<thead>
<tr><th>Item</th><th>Owner</th><th>Priority</th><th>Status</th></tr>
</thead>
<tbody>
<tr><td>Relax the <code>normal_status</code> binding from <code>required</code> to <code>extensible</code></td><td>openEHR</td><td><code>high</code></td><td>In progress. Ticket filed November 2025; the principle is agreed.</td></tr>
<tr><td>Merge <code>LINK</code> and <code>PARTY_IDENTIFIED</code> onto a common Reference Model class</td><td>openEHR</td><td><code>medium</code></td><td>Open. Strong consensus on the need; <code>OBJECT_REF</code> identified as the right structure. A Reference Model change is required. <strong>This guide maps the RM as published today.</strong></td></tr>
<tr><td>A <code>DV_IDENTIFIER</code> cluster archetype carrying <code>use</code>, <code>period</code>, and several types</td><td>Modelling team</td><td><code>medium</code></td><td>Done; needs to be uploaded for review.</td></tr>
<tr><td>A narrative cluster archetype for the <code>COMPOSITION</code> extension slot</td><td>Modelling team</td><td><code>medium</code></td><td>Done; needs to be uploaded for review.</td></tr>
<tr><td>Constraining <code>units_system</code> in Archetype Designer, and an RM <code>Money</code> type</td><td>Ian</td><td><code>low</code></td><td>Will not do as an archetype — implementation guidance is more appropriate, because the tooling does not currently allow the binding to be set even though ADL permits it. Two change requests are open instead.</td></tr>
<tr><td>Add whitespace restrictions to <code>CODE_PHRASE.code_string</code></td><td>openEHR</td><td><code>low</code></td><td>Proposed September 2025; the group agreed to propose it. No ticket confirmed. Without it, a <code>code_string</code> containing whitespace has <strong>no valid FHIR <code>code</code> form at all</strong>, so this guide produces nothing for one rather than an instance a validator rejects — see the <code>code_string[whitespace]</code> row on <a href="mapping-coded.html">Coded Data</a>.</td></tr>
<tr><td>Relax the <code>TERM_MAPPING.purpose</code> binding from <code>required</code> to <code>extensible</code></td><td>openEHR IG</td><td><code>medium</code></td><td>In progress. A binding-relaxation ticket was filed November 2025; the specific <code>TERM_MAPPING</code> change is not confirmed. The current <code>required</code> binding is considered incorrect.</td></tr>
<tr><td>Support both FHIR value sets — data-absent-reason and v3 NullFlavor</td><td>openEHR</td><td><code>medium</code></td><td>Planned; agreed February 2026. The back-transformation is keyed on which code system is present. ConceptMaps not yet created.</td></tr>
</tbody>
</table>
</div>

#### Documentation and tooling

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid">
<thead>
<tr><th>Item</th><th>Owner</th><th>Priority</th><th>Status</th></tr>
</thead>
<tbody>
<tr><td>Publish an ISO 8601 ↔ UCUM duration conversion reference library</td><td>Severin</td><td><code>medium</code></td><td>In progress. The conversion table was delivered February 2026 and is implemented in this guide’s reference implementation; a standalone library is not yet published. If one is, it <strong>supersedes</strong> the reference helper rather than conflicting with it.</td></tr>
<tr><td>Write the ConceptMaps for null_flavour ↔ data-absent-reason and ↔ NullFlavor</td><td>Group</td><td><code>medium</code></td><td>Open; agreed as needed February 2026. The narrative correspondence tables in <a href="mapping-coded.html">Coded Data</a> are the input, so the remaining work is transcription.</td></tr>
<tr><td>A standardised <code>DV_AMOUNT</code> mapping pattern document</td><td>Group</td><td><code>high</code></td><td><strong>Addressed by this guide.</strong> The policy — every inherited field promotes to the <code>Observation</code> level — was agreed January 2026, and the pattern is now stated once on <a href="cross-cutting.html">Cross-Cutting Concerns</a> and applied per type.</td></tr>
<tr><td>Choose the <code>system</code> + <code>version</code> → <code>terminology_id</code> format</td><td>Group</td><td><code>high</code></td><td>Open. Trade-offs discussed September 2025; the pipe form has support; the decision is deferred. <strong>This guide adopts nothing</strong> — the reference helpers take the format as a required parameter with no default, and no registered converter calls them.</td></tr>
<tr><td>A default strategy for a missing mandatory <code>terminology_id</code> or <code>code_string</code></td><td>Group</td><td><code>high</code></td><td>Open. Options brainstormed September 2025 and February 2026; no formal decision. The candidates are stated on <a href="mapping-coded.html">Coded Data</a> and none is chosen.</td></tr>
<tr><td>A detailed ISO 8601 subset comparison</td><td>Group</td><td><code>medium</code></td><td><strong>Addressed by this guide.</strong> Acknowledged as roughly 90% aligned in January 2026; the comparison is now published on <a href="cross-cutting.html">Cross-Cutting Concerns</a>, generated from the code that implements the conversion.</td></tr>
<tr><td>Document the general case of cross-version extensions</td><td>Gino</td><td><code>medium</code></td><td>Open. The agreed handling: where FHIR supplies no value but does supply an extension, use the value from the extension; for coded values, use <code>TERM_MAPPING</code> to carry the several values.</td></tr>
<tr><td>Decide how <code>DV_PARSABLE.formalism</code> reaches FHIR, if it can</td><td>Group</td><td><code>high</code></td><td>Reopened by review. The working group recorded a <strong>Decision made</strong> to carry the formalism in the <code>mimeType</code> extension. That extension declares its context as <code>Questionnaire.item</code> and <code>ElementDefinition</code> and exists to constrain the attachments an element permits, so it cannot carry an instance’s syntax and the decision is not supported by the definition it names. <strong>This guide publishes the gap</strong> rather than an instance no validator accepts, which makes <code>DV_PARSABLE ↔ string</code> one-directional until the question is settled.</td></tr>
<tr><td>Decide where a <code>DV_TIME</code> UTC offset goes, given FHIR <code>time</code> has no home for one</td><td>Group</td><td><code>high</code></td><td>Reopened by review. The <code>timezone</code> extension admits <code>time</code> as a context but is a <code>code</code> <strong>required</strong>-bound to the IANA zone names, and an offset is neither a zone name nor derivable from one. The guide therefore publishes the offset as a named drop and recommends <code>dateTime</code> where the offset is clinically significant. Whether to request a FHIR extension that carries an offset on a <code>time</code> is undecided.</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: open-items (generated by npm --prefix reference run render) <<< -->
