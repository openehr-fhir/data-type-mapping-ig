### Open items and review coverage

This guide is a work in progress, and it says so in detail rather than in the
abstract. This page publishes two things the working group needs and readers
deserve:

- **Reviewer coverage** — which mappings have been reviewed from the openEHR
  side, from the FHIR side, from both, or from neither. Coverage is uneven. A
  mapping reviewed on one side only carries more risk of an unsupported claim
  than one reviewed on both, and hiding that would misrepresent the guide's
  maturity.
- **The open-items register** — every unresolved action, who owns it, and what
  its current status is.

Open items fall into three groups.

**FHIR-side actions** are HL7 Jira tickets against the FHIR specification
itself: changes to `Quantity.comparator`, to `Attachment`, and to the
terminology guidance this guide depends on. This guide **references** their
status; it does not advance them, and it does not publish an R6-era item as
though it were R5 content.

**openEHR-side actions** are changes to the openEHR Reference Model or to its
published terminology, owned by the openEHR modelling team: binding strengths,
`code_string` constraints, `TERM_MAPPING.purpose`, and the pending
`LINK` / `PARTY_IDENTIFIED` / `OBJECT_REF` unification.

**Documentation and tooling actions** are this guide's own backlog — the
concept-map pass, the archetypes that several gap rows depend on, and the
questions the working group has not yet decided.

Every ticket cited anywhere in a mapping row appears in the register below.
That is enforced mechanically, so a gap cannot cite an owner that this page then
fails to list.

An item's presence here is not an admission of a defect. It is the guide
declining to assert something nobody has agreed.

See [Gaps](gaps.html) for the mapping-level consequences of these items, and
[Conventions](conventions.html) for what `open` and `not-discussed` mean.

### Reviewer coverage

Which mappings have been reviewed, and from which side. **unchecked** means exactly
that: nobody has reviewed the mapping from that side, and a claim in it carries more
risk than one that has been checked twice.

<!-- >>> mapping ledger: review-coverage (generated by npm --prefix reference run render) >>> -->
| Mapping | Category | openEHR review | FHIR review |
|-|-|-|-|
| [DV_QUANTITY ↔ Quantity](mapping-quantity.html) | Quantities | ✓ Diego, Ian | ✓ Gino |
| [DV_COUNT ↔ Count](mapping-quantity.html) | Quantities | ✓ Diego, Ian | ✓ Gino |
| [DV_PROPORTION ↔ Ratio](mapping-quantity.html) | Quantities | ✓ Diego, Ian | ✓ Gino |
| [DV_INTERVAL ↔ Range / Period / Quantity](mapping-quantity.html) | Quantities | ✓ Diego, Ian | ✓ Gino |
| [DV_QUANTITY ↔ Money / MoneyQuantity](mapping-quantity.html) | Quantities | **unchecked** | **unchecked** |
| [DV_QUANTITY ↔ SimpleQuantity](mapping-quantity.html) | Quantities | ✓ Diego, Ian | **unchecked** |
| [DV_ORDINAL ↔ Observation.component](mapping-quantity.html) | Quantities | ✓ Diego, Ian | ✓ Gino |
| [DV_SCALE ↔ Observation.component](mapping-quantity.html) | Quantities | ✓ Diego, Ian | ✓ Gino |
| [CODE_PHRASE ↔ Coding](mapping-coded.html) | Coded Data | ✓ Severin | ✓ Gino |
| [DV_CODED_TEXT ↔ CodeableConcept / Coding](mapping-coded.html) | Coded Data | ✓ Severin | ✓ Gino |
| [TERM_MAPPING ↔ CodeableConcept.coding](mapping-coded.html) | Coded Data | ✓ Severin | ✓ Gino |
| [null_flavour ↔ data-absent-reason](mapping-coded.html) | Coded Data | ✓ Severin | **unchecked** |
| [DV_BOOLEAN ↔ boolean](mapping-boolean.html) | Boolean Data | **unchecked** | **unchecked** |
| [Integer ↔ integer](mapping-numeric.html) | Numeric Primitives | **unchecked** | **unchecked** |
| [Integer64 ↔ integer64](mapping-numeric.html) | Numeric Primitives | **unchecked** | **unchecked** |
| [Real / Double ↔ decimal](mapping-numeric.html) | Numeric Primitives | **unchecked** | **unchecked** |
| [DV_TEXT ↔ string / markdown](mapping-textual.html) | Textual Data | ✓ Ciprian | ✓ Gino |
| [DV_PARAGRAPH ↔ markdown / string](mapping-textual.html) | Textual Data | **unchecked** | **unchecked** |
| [DV_IDENTIFIER ↔ Identifier](mapping-reference.html) | Resource-Locator Data and References | ✓ Severin | ✓ Gino |
| [DV_URI / DV_EHR_URI ↔ uri / url](mapping-reference.html) | Resource-Locator Data and References | **unchecked** | **unchecked** |
| [LINK ↔ Reference / CodeableReference](mapping-reference.html) | Resource-Locator Data and References | ✓ Severin | **unchecked** |
| [DV_DATE ↔ date / dateTime](mapping-temporal.html) | Temporal Data | ✓ Ciprian | ✓ Gino |
| [DV_TIME ↔ time](mapping-temporal.html) | Temporal Data | ✓ Ciprian | ✓ Gino |
| [DV_DATE_TIME ↔ dateTime / instant](mapping-temporal.html) | Temporal Data | ✓ Ciprian | ✓ Gino |
| [DV_DURATION ↔ Duration](mapping-temporal.html) | Temporal Data | ✓ Diego, Ian, Ciprian | **unchecked** |
| [Time specifications ↔ Timing](mapping-temporal.html) | Temporal Data | **unchecked** | **unchecked** |
| [DV_MULTIMEDIA ↔ Attachment](mapping-other.html) | Other Data | **unchecked** | **unchecked** |
| [DV_PARSABLE ↔ string](mapping-other.html) | Other Data | **unchecked** | **unchecked** |
| [DV_STATE ↔ CodeableConcept](mapping-other.html) | Other Data | ✓ Severin | **unchecked** |
| [FHIR types with no openEHR counterpart](gaps.html) | Gaps | **unchecked** | **unchecked** |
| [openEHR constructs not yet discussed](gaps.html) | Gaps | **unchecked** | **unchecked** |
<!-- <<< mapping ledger: review-coverage (generated by npm --prefix reference run render) <<< -->

### The register

Every ticket cited anywhere in a mapping row appears below. That is enforced
mechanically, so a gap cannot blame an owner this page then fails to list.

<!-- >>> mapping ledger: open-items (generated by npm --prefix reference run render) >>> -->
#### FHIR-side actions

| Item | Owner | Priority | Status |
|-|-|-|-|
| Add `defining_code` to the `coding-purpose` code system <br/>[HTA-170](https://jira.hl7.org/browse/HTA-170) | Brett Esler | `high` | Open. Submitted to HL7 Terminology. The group is trending towards the pragmatic position that `userSelected` and `defining_code` are equivalent enough, and may withdraw the request. |
| Add `~` (approximate) to `Quantity.comparator` <br/>[FHIR-56000](https://jira.hl7.org/browse/FHIR-56000) | Gino | `medium` | Resolved, not applied. Lands in R6 with guidance on stating an accuracy range and a default assumption of ±10%. Not available in an R5 instance. |
| A hash-algorithm extension on `Attachment` <br/>[FHIR-55422](https://jira.hl7.org/browse/FHIR-55422) | Gino | `medium` | Resolved, change required. HL7 agreed to define a core `alternate-hash` extension while keeping SHA-1 as the default. Not yet published. |
| A compression-algorithm extension on `Attachment` <br/>[FHIR-56003](https://jira.hl7.org/browse/FHIR-56003) | Gino | `low` | Triaged. The pragmatic alternative is to decompress during conversion, which removes the need for the field entirely. |
| A FHIR representation for `pk_fraction` and `pk_integer_fraction` formatting <br/>[FHIR-56001](https://jira.hl7.org/browse/FHIR-56001) | Gino | `medium` | Triaged. These are display directives; a search of `Ratio` found nothing suitable, and the desire is to use the standard `rendered-value` extension. |
| A thumbnail extension on `Attachment` <br/>[FHIR-56002](https://jira.hl7.org/browse/FHIR-56002) | Gino | `medium` | Triaged. The existing `documentreference-thumbnail` extension is a flag on `DocumentReference.content` saying the content *is* a thumbnail; a new extension on `Attachment` was requested instead. |

#### openEHR-side actions

| Item | Owner | Priority | Status |
|-|-|-|-|
| Relax the `normal_status` binding from `required` to `extensible` | openEHR | `high` | In progress. Ticket filed November 2025; the principle is agreed. |
| Merge `LINK` and `PARTY_IDENTIFIED` onto a common Reference Model class | openEHR | `medium` | Open. Strong consensus on the need; `OBJECT_REF` identified as the right structure. A Reference Model change is required. **This guide maps the RM as published today.** |
| A `DV_IDENTIFIER` cluster archetype carrying `use`, `period`, and several types | Modelling team | `medium` | Done; needs to be uploaded for review. |
| A narrative cluster archetype for the `COMPOSITION` extension slot | Modelling team | `medium` | Done; needs to be uploaded for review. |
| Constraining `units_system` in Archetype Designer, and an RM `Money` type | Ian | `low` | Will not do as an archetype — implementation guidance is more appropriate, because the tooling does not currently allow the binding to be set even though ADL permits it. Two change requests are open instead. |
| Add whitespace restrictions to `CODE_PHRASE.code_string` | openEHR | `low` | Proposed September 2025; the group agreed to propose it. No ticket confirmed. Without it, a `code_string` containing whitespace has no valid FHIR `code` form. |
| Relax the `TERM_MAPPING.purpose` binding from `required` to `extensible` | openEHR IG | `medium` | In progress. A binding-relaxation ticket was filed November 2025; the specific `TERM_MAPPING` change is not confirmed. The current `required` binding is considered incorrect. |
| Support both FHIR value sets — data-absent-reason and v3 NullFlavor | openEHR | `medium` | Planned; agreed February 2026. The back-transformation is keyed on which code system is present. ConceptMaps not yet created. |

#### Documentation and tooling

| Item | Owner | Priority | Status |
|-|-|-|-|
| Publish an ISO 8601 ↔ UCUM duration conversion reference library | Severin | `medium` | In progress. The conversion table was delivered February 2026 and is implemented in this guide’s reference implementation; a standalone library is not yet published. If one is, it **supersedes** the reference helper rather than conflicting with it. |
| Write the ConceptMaps for null_flavour ↔ data-absent-reason and ↔ NullFlavor | Group | `medium` | Open; agreed as needed February 2026. The narrative correspondence tables in [Coded Data](mapping-coded.html) are the input, so the remaining work is transcription. |
| A standardised `DV_AMOUNT` mapping pattern document | Group | `high` | **Addressed by this guide.** The policy — every inherited field promotes to the `Observation` level — was agreed January 2026, and the pattern is now stated once on [Cross-Cutting Concerns](cross-cutting.html) and applied per type. |
| Choose the `system` + `version` → `terminology_id` format | Group | `high` | Open. Trade-offs discussed September 2025; the pipe form has support; the decision is deferred. **This guide adopts nothing** — the reference helpers take the format as a required parameter with no default, and no registered converter calls them. |
| A default strategy for a missing mandatory `terminology_id` or `code_string` | Group | `high` | Open. Options brainstormed September 2025 and February 2026; no formal decision. The candidates are stated on [Coded Data](mapping-coded.html) and none is chosen. |
| A detailed ISO 8601 subset comparison | Group | `medium` | **Addressed by this guide.** Acknowledged as roughly 90% aligned in January 2026; the comparison is now published on [Cross-Cutting Concerns](cross-cutting.html), generated from the code that implements the conversion. |
| Document the general case of cross-version extensions | Gino | `medium` | Open. The agreed handling: where FHIR supplies no value but does supply an extension, use the value from the extension; for coded values, use `TERM_MAPPING` to carry the several values. |
| Decide how `DV_PARSABLE.formalism` reaches FHIR, if it can | Group | `high` | Reopened by review. The working group recorded a **Decision made** to carry the formalism in the `mimeType` extension. That extension declares its context as `Questionnaire.item` and `ElementDefinition` and exists to constrain the attachments an element permits, so it cannot carry an instance’s syntax and the decision is not supported by the definition it names. **This guide publishes the gap** rather than an instance no validator accepts, which makes `DV_PARSABLE ↔ string` one-directional until the question is settled. |
| Decide where a `DV_TIME` UTC offset goes, given FHIR `time` has no home for one | Group | `high` | Reopened by review. The `timezone` extension admits `time` as a context but is a `code` **required**-bound to the IANA zone names, and an offset is neither a zone name nor derivable from one. The guide therefore publishes the offset as a named drop and recommends `dateTime` where the offset is clinically significant. Whether to request a FHIR extension that carries an offset on a `time` is undecided. |
<!-- <<< mapping ledger: open-items (generated by npm --prefix reference run render) <<< -->
