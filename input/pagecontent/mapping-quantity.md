### Quantities

openEHR's quantitative data flows through an **inheritance chain**, not through
one class: `DV_ORDERED` → `DV_QUANTIFIED` → `DV_AMOUNT` → `DV_QUANTITY`, with
`DV_COUNT`, `DV_PROPORTION`, and `DV_DURATION` branching off it. Each level
contributes its own attributes, and citing the right one matters because a
reader following a citation expects to find the attribute declared there:

- **`DV_ORDERED`** declares `normal_status`, `normal_range`, and
  `other_reference_ranges`.
- **`DV_QUANTIFIED`** adds `magnitude_status`.
- **`DV_AMOUNT`** adds `accuracy` and `accuracy_is_percent`.

Those six attributes follow the same mapping strategy regardless of the
concrete subtype. That shared strategy is described once, as the
**`DV_AMOUNT` pattern**, in [Cross-Cutting Concerns](cross-cutting.html), and
each type's table then states how it applies.

FHIR spreads the same ground across more types. Single values normally use the
`Quantity` branch — `Quantity`, `Age`, `Distance`, `Duration`, `Count`,
`MoneyQuantity`, `SimpleQuantity` — whose members are *restrictions* on the base
type rather than independent types. Other quantitative types include `Ratio`,
`Range`, `Period`, `RatioRange`, `Timing`, `RelativeTime`, and `Money`.
`RelativeTime` is an R6-era type with **no openEHR counterpart identified**; it
is listed on [Gaps](gaps.html) rather than mapped here.

Four facts govern the rows in this category.

**Reference ranges and interpretation are resource-level.** `normal_range`,
`other_reference_ranges`, and `normal_status` are attributes of an openEHR
*value*. Their FHIR counterparts — `Observation.referenceRange` and
`Observation.interpretation` — are elements of a *resource*. Rows for them are
`archetype` scope, not `datatype` scope.

**Precision is carried differently on each side.** openEHR uses `precision`
together with the lexical form of `magnitude`; FHIR uses the lexical form of
`decimal` together with the `quantity-precision` extension. A mapping SHOULD
take the most granular precision available across both fields. openEHR
`precision = -1` (unlimited) corresponds to the absence of the extension.

**Profiled quantities add invariants a `DV_QUANTITY` may violate.**
`SimpleQuantity` forbids a comparator; `Age`, `Distance`, `Duration`, and
`Count` constrain `system` and `code`; `Money` and `MoneyQuantity` carry a
currency. A `DV_QUANTITY` with a `magnitude_status` cannot be carried in a
`SimpleQuantity` element at all — that is a modelling error, not a lossy
mapping.

**Units are mandatory on the FHIR side more often than on the openEHR side.**
FHIR invariant `qty-3` requires `system` whenever `code` is present, so a
mapping engine SHALL supply `http://unitsofmeasure.org` when openEHR
`units_system` is absent and the units are UCUM. `Count` requires
`system = http://unitsofmeasure.org` with `code = 1`; `Money` uses
`system = urn:iso:std:iso:4217` with the ISO 4217 currency code.

`DV_ORDINAL` and `DV_SCALE` are mapped here rather than in
[Coded Data](mapping-coded.html): although both carry a `CODE_PHRASE` symbol,
their FHIR target is driven by the ordinal magnitude and neither is expressible
without an `Observation` around it.

Date and time intervals are in [Temporal Data](mapping-temporal.html); only
numeric intervals are here.

See [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:quantity (generated by npm --prefix reference run render) >>> -->

<table>
<thead>
<tr><th>openEHR type</th><th>FHIR type</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Scope</th></tr>
</thead>
<tbody>
<tr><td><code>DV_QUANTITY</code></td><td><code>Quantity</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_COUNT</code></td><td><code>Count</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_PROPORTION</code></td><td><code>Ratio</code></td><td><code>lossy</code></td><td><code>unmapped</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_INTERVAL&lt;T&gt;</code></td><td><code>Range | Period | Quantity</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>settled</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_QUANTITY</code></td><td><code>Money</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_QUANTITY</code></td><td><code>SimpleQuantity</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>settled</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_ORDINAL</code></td><td><code>Observation.component</code></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>archetype</code></td></tr>
<tr><td><code>DV_SCALE</code></td><td><code>Observation.component</code></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>archetype</code></td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: summary:quantity (generated by npm --prefix reference run render) <<< -->

### DV_QUANTITY and Quantity

<!-- >>> mapping ledger: mapping:dv-quantity-to-quantity (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">openEHR RM — DV_QUANTITY</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">FHIR R5 — Quantity</a> · <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">openEHR RM — DV_ORDERED</a> · <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantified_class">openEHR RM — DV_QUANTIFIED</a> · <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_amount_class">openEHR RM — DV_AMOUNT</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.magnitude</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.value</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>Direct equivalence. <code>magnitude</code> is mandatory in openEHR and optional in FHIR, so a <code>Quantity</code> with no <code>value</code> has no <code>DV_QUANTITY</code> to become. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.units</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.code</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>The computable unit, normally a UCUM code. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.units_system</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.system</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>FHIR invariant <code>qty-3</code> requires <code>system</code> whenever <code>code</code> is present, so a mapping engine SHALL supply <code>http://unitsofmeasure.org</code> when <code>units_system</code> is absent and the units are UCUM. The field itself round-trips exactly whenever it is present; a supplied default arrives back as an explicit <code>units_system</code>, which is a normalisation rather than a loss.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.units_display_name</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.unit</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>The human-readable unit label, for example <code>°C</code>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.precision</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-quantity-precision.html">Quantity.extension[quantity-precision]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>openEHR <code>precision = -1</code> (unlimited) is equivalent to the <strong>absence</strong> of the extension in FHIR. Take the most granular precision available across the value and this field in both directions.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantified_class">DV_QUANTITY.magnitude_status</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.comparator</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>, and <code>&gt;=</code> are direct. openEHR <code>=</code> is the point value and is represented in FHIR by the <strong>absence</strong> of <code>comparator</code>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantified_class">DV_QUANTITY.magnitude_status[~]</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: An approximate magnitude cannot be carried in an R5 <code>Quantity</code>. FHIR-56000 is resolved and adds <code>~</code> in R6, with guidance to state an accuracy range and a default assumption of ±10%; it is not applied in R5. (owner: <a href="https://jira.hl7.org/browse/FHIR-56000">FHIR-56000</a>) → openEHR: Nothing in an R5 instance carries the approximate marker back. (owner: <a href="https://jira.hl7.org/browse/FHIR-56000">FHIR-56000</a>) R6-era item, recorded as a forward reference and <strong>not</strong> presented as an R5 mapping.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_amount_class">DV_QUANTITY.accuracy</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-quantity-accuracy.html">Quantity.extension[quantity-accuracy]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>open</code></td><td>Inherited from <code>DV_AMOUNT</code>. Agreed as the mapping target but rarely used in practice. The extension declares <code>value[x]</code> as a <strong><code>Quantity</code></strong>, not a <code>decimal</code>, so <code>accuracy_is_percent</code> is carried too: an accuracy stated as a percentage takes UCUM <code>%</code> as the accuracy quantity’s unit, and an absolute one takes the magnitude’s own unit. Reading the extension back, <code>accuracy_is_percent</code> is <code>true</code> exactly when the accuracy quantity’s code is <code>%</code> — <strong>except</strong> where the magnitude’s own unit is <code>%</code>, where the two cases are indistinguishable and the flag is not carried at all. That sub-case is the next row.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_amount_class">DV_QUANTITY.accuracy_is_percent[percent-unit]</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-quantity-accuracy.html">Quantity.extension[quantity-accuracy][percent-unit]</a><sup>†</sup> <em>(extension)</em></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: The flag is not carried. The <code>quantity-accuracy</code> extension’s <strong>only</strong> discriminator is the unit of the accuracy <code>Quantity</code>, and where the magnitude’s own unit is already <code>%</code> that unit is what an absolute accuracy takes <em>and</em> what a relative one takes. The accuracy magnitude is still emitted; the flag beside it is not. (owner: <code>working-group</code>) → openEHR: Nothing in the instance says whether the accuracy is absolute or relative, so <code>accuracy_is_percent</code> is left <strong>absent</strong> rather than derived. Deriving it would read ±2 percentage points on a 45 % value as ±2 % <em>of</em> 45 — which is ±0.9, a different number. (owner: <code>working-group</code>) A named sub-case of the <code>DV_QUANTITY.accuracy</code> row above, in the pattern the guide already uses for <code>DV_QUANTITY.magnitude_status[~]</code> and <code>CODE_PHRASE.code_string[whitespace]</code>. An SpO₂ or haematocrit of 45 % with an absolute accuracy of ±2 percentage points is the ordinary clinical shape. Resolving it needs a discriminator the extension does not have — a second extension, or a <code>value[x]</code> that is not a <code>Quantity</code> — so the gap is published rather than closed by a convention this guide would be inventing.</td></tr>
<tr><td>—<br/><em>no counterpart</em> (<a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">inventory</a>)</td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.comparator[ad]</a></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: No openEHR value produces <code>ad</code>. (owner: <code>working-group</code>) → openEHR: A <code>Quantity</code> with <code>comparator = ad</code> has no faithful <code>DV_QUANTITY</code> form; the comparator is dropped and the magnitude alone would misstate the value. (owner: <code>working-group</code>) The group read the R5 definition — "the quantity is sufficient for the total quantity to equal the stated amount" — as unclear, and the question of whether it means "approximate" was asked of FHIR and not answered.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_QUANTITY.normal_range</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.referenceRange">Observation.referenceRange</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>archetype</code> scope: the FHIR home is a resource element, not part of <code>Quantity</code>. Carried with <code>referenceRange.type = normal</code>. Inherited from <code>DV_AMOUNT</code> and mapped identically for every subtype.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_QUANTITY.other_reference_ranges</a> <code>0..*</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.referenceRange">Observation.referenceRange</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>archetype</code> scope. <code>REFERENCE_RANGE.meaning</code> becomes <code>referenceRange.type</code>, which is <code>≠ normal</code> for these.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_QUANTITY.normal_status</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.interpretation">Observation.interpretation</a></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ openEHR: drops <code>Observation.interpretation</code> — openEHR binds <code>normal_status</code> to its <code>normal_statuses</code> code system with <code>required</code> strength, so an interpretation coded outside that set cannot be carried; a change request to relax the binding to <code>extensible</code> is open <code>archetype</code> scope. The openEHR code system derives from an older HL7 v2 release and maps cleanly for the H/L/N codes; a full ConceptMap has not been written.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-quantity-to-quantity (generated by npm --prefix reference run render) <<< -->

#### Worked example - body weight

A `DV_QUANTITY` carrying a magnitude, UCUM units, a display label, and a precision
of one decimal place:

<!-- >>> mapping ledger: example:dv-quantity-to-quantity/01-body-weight.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_QUANTITY",
  "magnitude": 72.5,
  "units": "kg",
  "units_system": "http://unitsofmeasure.org",
  "units_display_name": "kg",
  "precision": 1
}
```

<!-- <<< mapping ledger: example:dv-quantity-to-quantity/01-body-weight.openehr.json (generated by npm --prefix reference run render) <<< -->

and the `Quantity` it becomes, with `precision` carried in the extension:

<!-- >>> mapping ledger: example:dv-quantity-to-quantity/01-body-weight.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "value": 72.5,
  "unit": "kg",
  "system": "http://unitsofmeasure.org",
  "code": "kg",
  "extension": [
    {
      "url": "http://hl7.org/fhir/StructureDefinition/quantity-precision",
      "valueInteger": 1
    }
  ]
}
```

<!-- <<< mapping ledger: example:dv-quantity-to-quantity/01-body-weight.fhir.json (generated by npm --prefix reference run render) <<< -->

#### Worked example - comparator and accuracy

<!-- >>> mapping ledger: example:dv-quantity-to-quantity/02-comparator-and-accuracy.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_QUANTITY",
  "magnitude": 0.5,
  "units": "mg",
  "units_system": "http://unitsofmeasure.org",
  "units_display_name": "mg",
  "magnitude_status": "<",
  "accuracy": 0.05,
  "accuracy_is_percent": false
}
```

<!-- <<< mapping ledger: example:dv-quantity-to-quantity/02-comparator-and-accuracy.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-quantity-to-quantity/02-comparator-and-accuracy.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "value": 0.5,
  "comparator": "<",
  "unit": "mg",
  "system": "http://unitsofmeasure.org",
  "code": "mg",
  "extension": [
    {
      "url": "http://hl7.org/fhir/StructureDefinition/quantity-accuracy",
      "valueQuantity": {
        "value": 0.05,
        "system": "http://unitsofmeasure.org",
        "code": "mg"
      }
    }
  ]
}
```

<!-- <<< mapping ledger: example:dv-quantity-to-quantity/02-comparator-and-accuracy.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_COUNT and Count

<!-- >>> mapping ledger: mapping:dv-count-to-count (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_count_class">openEHR RM — DV_COUNT</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Count">FHIR R5 — Count</a> · <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">openEHR RM — DV_ORDERED</a> · <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantified_class">openEHR RM — DV_QUANTIFIED</a> · <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_amount_class">openEHR RM — DV_AMOUNT</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_count_class">DV_COUNT.magnitude</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Count">Count.value</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>FHIR <code>Count</code> invariant <code>cnt-3</code> requires a whole number. Both sides are 32-bit integers in practice, so the range is the same. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantified_class">DV_COUNT.magnitude_status</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Count">Count.comparator</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>The same comparator correspondence as <code>DV_QUANTITY.magnitude_status</code>.</td></tr>
<tr><td>—<br/><em>no counterpart</em> (<a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_count_class">inventory</a>)</td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Count">Count.system</a></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: No openEHR field produces it. FHIR invariant <code>cnt-3</code> fixes it to <code>http://unitsofmeasure.org</code>, so a mapping engine supplies the constant. → openEHR: The fixed UCUM system carries no information into openEHR.</td></tr>
<tr><td>—<br/><em>no counterpart</em> (<a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_count_class">inventory</a>)</td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Count">Count.code</a></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: No openEHR field produces it. FHIR invariant <code>cnt-3</code> fixes it to <code>1</code>, so a mapping engine supplies the constant. → openEHR: The fixed UCUM code <code>1</code> carries no information into openEHR.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_COUNT.normal_range</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.referenceRange">Observation.referenceRange</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>archetype</code> scope. <code>DV_COUNT</code> inherits <code>normal_range</code> and <code>other_reference_ranges</code> from <code>DV_ORDERED</code> like every other ordered value, and both share one <code>Observation.referenceRange</code> home and one verdict, which is why they are stated together here. <code>normal_status</code> is <strong>not</strong> folded in with them: its FHIR home and its verdict are different, and it has a row of its own below.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_COUNT.normal_status</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.interpretation">Observation.interpretation</a></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ openEHR: drops <code>Observation.interpretation</code> — openEHR binds <code>normal_status</code> to its <code>normal_statuses</code> code system with <code>required</code> strength, so an interpretation coded outside that set cannot be carried; a change request to relax the binding to <code>extensible</code> is open <code>archetype</code> scope. Inherited from <code>DV_ORDERED</code> — § 6.2.1 is where <code>normal_status</code> is declared — and it follows the <code>DV_QUANTITY</code> pattern <strong>including that pattern’s inbound loss</strong>, which is why it is a row of its own rather than a sentence on the reference-range row. <code>maturity</code> is <code>open</code>, not <code>settled</code>, because the change request to relax the binding is still open, exactly as for the <code>DV_QUANTITY</code>, <code>DV_ORDINAL</code>, and <code>DV_SCALE</code> rows that state the same attribute.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-count-to-count (generated by npm --prefix reference run render) <<< -->

#### Worked example - number of pregnancies

<!-- >>> mapping ledger: example:dv-count-to-count/01-pregnancies.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_COUNT",
  "magnitude": 3
}
```

<!-- <<< mapping ledger: example:dv-count-to-count/01-pregnancies.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-count-to-count/01-pregnancies.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "value": 3,
  "system": "http://unitsofmeasure.org",
  "code": "1"
}
```

<!-- <<< mapping ledger: example:dv-count-to-count/01-pregnancies.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_PROPORTION and Ratio

<!-- >>> mapping ledger: mapping:dv-proportion-to-ratio (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_proportion_class">openEHR RM — DV_PROPORTION</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Ratio">FHIR R5 — Ratio</a> · <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_proportion_kind_class">openEHR RM — PROPORTION_KIND</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_proportion_class">DV_PROPORTION.numerator</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Ratio">Ratio.numerator.value</a></td><td><code>lossless</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ openEHR: Nothing is produced. <code>DV_PROPORTION.type</code> is mandatory and no <code>Ratio</code> carries a kind discriminator, so no <code>Ratio</code> becomes a <code>DV_PROPORTION</code> at all and the numerator has nothing to land in. The value itself would carry across; it does not carry across on its own. (owner: <a href="https://jira.hl7.org/browse/FHIR-56001">FHIR-56001</a>) FHIR carries the numerator as a <code>Quantity</code>; openEHR carries a bare decimal.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_proportion_class">DV_PROPORTION.denominator</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Ratio">Ratio.denominator.value</a></td><td><code>lossless</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ openEHR: Nothing is produced, for the same reason as the numerator: <code>DV_PROPORTION.type</code> is mandatory and unsourceable from a <code>Ratio</code>, so the denominator has nothing to land in. (owner: <a href="https://jira.hl7.org/browse/FHIR-56001">FHIR-56001</a>) <code>pk_unitary</code> fixes the denominator to <code>1</code> and <code>pk_percent</code> fixes it to <code>100</code>; both are carried as ordinary denominator values.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_proportion_kind_class">DV_PROPORTION.type</a> <code>1..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Ratio">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: The kind discriminator is dropped. <code>pk_unitary</code> and <code>pk_percent</code> survive implicitly through their fixed denominators, but <code>pk_fraction</code> and <code>pk_integer_fraction</code> are display directives with no FHIR home. (owner: <a href="https://jira.hl7.org/browse/FHIR-56001">FHIR-56001</a>) → openEHR: <code>DV_PROPORTION.type</code> is mandatory, so an incoming <code>Ratio</code> requires the mapping engine to infer a kind from the denominator — <code>1</code> implies <code>pk_unitary</code>, <code>100</code> implies <code>pk_percent</code>, anything else <code>pk_ratio</code> — which is an inference, not a carried value. (owner: <a href="https://jira.hl7.org/browse/FHIR-56001">FHIR-56001</a>) The desire is to use the standard <code>rendered-value</code> extension; the group found nothing on <code>Ratio</code> for fraction display and concluded an extension may be needed. The reference implementation <strong>refuses rather than infers</strong>: because the kind is mandatory and unsourceable, <code>ratioToDvProportion</code> produces no <code>DV_PROPORTION</code> at all, which is why the numerator, denominator and precision rows are <code>unmapped</code> inbound too.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_proportion_class">DV_PROPORTION.precision</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-quantity-precision.html">Ratio.numerator.extension[quantity-precision]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossless</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ openEHR: Nothing is produced. <code>DV_PROPORTION.type</code> is mandatory and unsourceable from a <code>Ratio</code>, so no <code>DV_PROPORTION</code> is produced and the precision extension has nothing to land in. (owner: <a href="https://jira.hl7.org/browse/FHIR-56001">FHIR-56001</a>) openEHR carries one precision for the whole proportion; FHIR would carry one per <code>Quantity</code>. This guide places it on the numerator and the reverse reads it from there, which round-trips but does not let the two sides differ.</td></tr>
<tr><td>—<br/><em>no counterpart</em> (<a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_proportion_class">inventory</a>)</td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Ratio">Ratio.numerator.code</a></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: No <code>DV_PROPORTION</code> field produces a unit. (owner: <code>openehr-modelling</code>) → openEHR: A <code>Ratio</code> carrying units — 5 mg per 100 mL — cannot be carried into a <code>DV_PROPORTION</code>. A change request to add optional numerator and denominator units to <code>DV_PROPORTION</code>, or a new proportion kind that enforces them, is open. (owner: <code>openehr-modelling</code>) The same applies to <code>Ratio.denominator.code</code>, <code>Ratio.numerator.system</code>, and <code>Ratio.denominator.system</code>.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-proportion-to-ratio (generated by npm --prefix reference run render) <<< -->

#### Worked example - a percentage

<!-- >>> mapping ledger: example:dv-proportion-to-ratio/01-percent.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_PROPORTION",
  "numerator": 45,
  "denominator": 100,
  "type": 2,
  "precision": 0
}
```

<!-- <<< mapping ledger: example:dv-proportion-to-ratio/01-percent.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-proportion-to-ratio/01-percent.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "numerator": {
    "value": 45,
    "extension": [
      {
        "url": "http://hl7.org/fhir/StructureDefinition/quantity-precision",
        "valueInteger": 0
      }
    ]
  },
  "denominator": {
    "value": 100
  }
}
```

<!-- <<< mapping ledger: example:dv-proportion-to-ratio/01-percent.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_INTERVAL and Range / Period / Quantity

<!-- >>> mapping ledger: mapping:dv-interval-to-range (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_interval_class">openEHR RM — DV_INTERVAL</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Range">FHIR R5 — Range</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Period">FHIR R5 — Period</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">FHIR R5 — Quantity</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_interval_class">DV_INTERVAL.lower</a> <code>0..1</code></td><td><em>when</em> the type parameter is <code>DV_QUANTITY</code> or <code>DV_COUNT</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#Range">Range.low</a><br/><em>when</em> the type parameter is <code>DV_DATE</code>, <code>DV_TIME</code>, or <code>DV_DATE_TIME</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#Period">Period.start</a><br/><em>when</em> only one bound is present and the interval is carried as a comparator <code>Quantity</code> rather than as a <code>Range</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.value</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>The FHIR target is chosen by the type parameter, not by the interval itself. <code>Period</code> and <code>Quantity</code> have search semantics in FHIR; <code>Range</code> does not. A bound the reference implementation cannot convert — a <code>SimpleQuantity</code> with no <code>value</code> or no <code>code</code> — stops the whole interval rather than being rewritten as zero; see <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_interval_class">DV_INTERVAL.upper</a> <code>0..1</code></td><td><em>when</em> the type parameter is <code>DV_QUANTITY</code> or <code>DV_COUNT</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#Range">Range.high</a><br/><em>when</em> the type parameter is <code>DV_DATE</code>, <code>DV_TIME</code>, or <code>DV_DATE_TIME</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#Period">Period.end</a><br/><em>when</em> only one bound is present and the interval is carried as a comparator <code>Quantity</code> rather than as a <code>Range</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#Quantity">Quantity.value</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td></td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_interval_class">DV_INTERVAL.lower_unbounded</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Range">Range.low</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>true</code> is represented by the <strong>absence</strong> of the corresponding boundary, which round-trips exactly. The working group confirmed from a CKM review that <code>*_unbounded</code> appears only in design-time constraints, not in instance data.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_interval_class">DV_INTERVAL.upper_unbounded</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Range">Range.high</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>true</code> is represented by the absence of <code>Range.high</code>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_interval_class">DV_INTERVAL.lower_included</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Range">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: An exclusive lower boundary cannot be stated in an instance. At design time it is expressed as a FHIRPath constraint in a profile, or by using <code>Quantity.comparator</code> instead of a <code>Range</code>. (owner: <code>working-group</code>) → openEHR: A FHIR instance never says whether a bound is exclusive, so the flag is set to the FHIR default — inclusive — rather than carried. (owner: <code>working-group</code>) Decided October 2025 on the evidence of a CKM review: instance data treats every boundary as inclusive. Exclusive boundaries do occur in real laboratory ranges but are rare and are a design-time concern.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_interval_class">DV_INTERVAL.upper_included</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Range">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: An exclusive upper boundary cannot be stated in an instance. (owner: <code>working-group</code>) → openEHR: A FHIR instance never says whether a bound is exclusive. (owner: <code>working-group</code>)</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-interval-to-range (generated by npm --prefix reference run render) <<< -->

#### Worked example - a reference range

<!-- >>> mapping ledger: example:dv-interval-to-range/01-reference-range.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_INTERVAL",
  "lower": {
    "_type": "DV_QUANTITY",
    "magnitude": 3.5,
    "units": "mmol/L",
    "units_system": "http://unitsofmeasure.org",
    "units_display_name": "mmol/L"
  },
  "upper": {
    "_type": "DV_QUANTITY",
    "magnitude": 5.0,
    "units": "mmol/L",
    "units_system": "http://unitsofmeasure.org",
    "units_display_name": "mmol/L"
  },
  "lower_unbounded": false,
  "upper_unbounded": false,
  "lower_included": true,
  "upper_included": true
}
```

<!-- <<< mapping ledger: example:dv-interval-to-range/01-reference-range.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-interval-to-range/01-reference-range.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "low": {
    "value": 3.5,
    "unit": "mmol/L",
    "system": "http://unitsofmeasure.org",
    "code": "mmol/L"
  },
  "high": {
    "value": 5.0,
    "unit": "mmol/L",
    "system": "http://unitsofmeasure.org",
    "code": "mmol/L"
  }
}
```

<!-- <<< mapping ledger: example:dv-interval-to-range/01-reference-range.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_QUANTITY and Money / MoneyQuantity

<!-- >>> mapping ledger: mapping:dv-quantity-to-money (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">openEHR RM — DV_QUANTITY</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Money">FHIR R5 — Money</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.magnitude</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Money">Money.value</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>open</code></td><td>The monetary amount itself. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.units</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Money">Money.currency</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>open</code></td><td>The ISO 4217 currency code is carried in <code>units</code>, with <code>units_system</code> set to <code>urn:iso:std:iso:4217</code>. <code>MoneyQuantity</code> is an ordinary <code>Quantity</code> profile and maps as <code>DV_QUANTITY ↔ Quantity</code> does. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.units_system</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Money">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: The constant <code>urn:iso:std:iso:4217</code> has no home on <code>Money</code> and is not carried. (owner: <code>openehr-modelling</code>) → openEHR: Nothing in a <code>Money</code> instance states the currency system, so the mapping engine supplies the constant rather than carrying it. (owner: <code>openehr-modelling</code>) Left to implementation guidance rather than to an archetype: the Archetype Designer tooling does not currently allow <code>units_system</code> to be constrained, even though ADL permits it. Two openEHR change requests are open — one for the tooling constraint, one proposing a <code>Money</code> data type in the RM.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-quantity-to-money (generated by npm --prefix reference run render) <<< -->

### DV_QUANTITY and SimpleQuantity

<!-- >>> mapping ledger: mapping:dv-quantity-to-simple-quantity (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">openEHR RM — DV_QUANTITY</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#SimpleQuantity">FHIR R5 — SimpleQuantity</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantity_class">DV_QUANTITY.magnitude</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#SimpleQuantity">SimpleQuantity.value</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>Every field except <code>magnitude_status</code> maps exactly as <code>DV_QUANTITY ↔ Quantity</code> does; <code>SimpleQuantity</code> is that mapping with <code>comparator</code> forbidden. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_quantified_class">DV_QUANTITY.magnitude_status</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#SimpleQuantity">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: A <code>DV_QUANTITY</code> carrying <code>magnitude_status</code> in a slot whose FHIR target is <code>SimpleQuantity</code> is a <strong>modelling error</strong> on the openEHR side. The expectation is that the mapping is refused, not that the status is silently dropped. → openEHR: A <code>SimpleQuantity</code> never carries a comparator, so nothing arrives.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-quantity-to-simple-quantity (generated by npm --prefix reference run render) <<< -->

### DV_ORDINAL and Observation.component

<!-- >>> mapping ledger: mapping:dv-ordinal-to-observation-component (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordinal_class">openEHR RM — DV_ORDINAL</a> · <a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.component">FHIR R5 — Observation.component</a></p>

<p><em>This mapping is <code>archetype</code> scope: it is not expressible between the two data types alone and needs the surrounding openEHR archetype and FHIR resource.</em></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordinal_class">DV_ORDINAL.symbol</a> <code>1..1</code></td><td><em>when</em> the ordinal is carried as an observation component: <a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.component">Observation.component.valueCodeableConcept</a><br/><em>when</em> the ordinal is an answer to a questionnaire item: <a href="https://hl7.org/fhir/R5/questionnaireresponse.html#QuestionnaireResponse.item.answer">QuestionnaireResponse.item.answer.valueCoding</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>The coded label of the ordinal value. See <a href="mapping-coded.html">Coded Data</a> for the <code>DV_CODED_TEXT</code> mapping itself.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordinal_class">DV_ORDINAL.value</a> <code>1..1</code></td><td><em>when</em> the score is carried as its own component value: <a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.component">Observation.component.valueInteger</a><br/><em>when</em> the score is carried as a weight on the coded symbol: <a href="https://hl7.org/fhir/extensions/StructureDefinition-itemWeight.html">Observation.component.valueCodeableConcept.coding.extension[itemWeight]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>A value carried in the <code>itemWeight</code> extension is unlikely to be surfaced by a FHIR search, which matters when choosing between the two shapes.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_ORDINAL.normal_range</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.referenceRange">Observation.referenceRange</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>archetype</code> scope. <code>DV_ORDINAL</code> inherits <code>normal_range</code> and <code>other_reference_ranges</code> from <code>DV_ORDERED</code> like every other ordered value, and both share one <code>Observation.referenceRange</code> home and one verdict, which is why they are stated together here. <code>normal_status</code> is <strong>not</strong> folded in with them: its FHIR home and its verdict are different, and it has a row of its own below. Without this row the aggregate would read <code>lossless</code> while a <code>DV_ORDINAL</code> carrying a <code>normal_range</code> had nowhere stated to put it.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_ORDINAL.normal_status</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.interpretation">Observation.interpretation</a></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ openEHR: drops <code>Observation.interpretation</code> — openEHR binds <code>normal_status</code> to its <code>normal_statuses</code> code system with <code>required</code> strength, so an interpretation coded outside that set cannot be carried; a change request to relax the binding to <code>extensible</code> is open <code>archetype</code> scope. Inherited from <code>DV_ORDERED</code> — § 6.2.1 is where <code>normal_status</code> is declared — and it follows the <code>DV_QUANTITY</code> pattern <strong>including that pattern’s inbound loss</strong>, which is why it is a row of its own rather than a sentence on the reference-range row.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-ordinal-to-observation-component (generated by npm --prefix reference run render) <<< -->

### DV_SCALE and Observation.component

<!-- >>> mapping ledger: mapping:dv-scale-to-observation-component (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_scale_class">openEHR RM — DV_SCALE</a> · <a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.component">FHIR R5 — Observation.component</a></p>

<p><em>This mapping is <code>archetype</code> scope: it is not expressible between the two data types alone and needs the surrounding openEHR archetype and FHIR resource.</em></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_scale_class">DV_SCALE.symbol</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.component">Observation.component.valueCodeableConcept</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>The same targets as <code>DV_ORDINAL.symbol</code>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_scale_class">DV_SCALE.value</a> <code>1..1</code></td><td><em>when</em> the score is carried as its own component value: <a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.component">Observation.component.valueQuantity</a><br/><em>when</em> the score is carried as a weight on the coded symbol: <a href="https://hl7.org/fhir/extensions/StructureDefinition-itemWeight.html">Observation.component.valueCodeableConcept.coding.extension[itemWeight]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>DV_SCALE.value</code> is a <code>Real</code>, so unlike <code>DV_ORDINAL.value</code> it cannot land in <code>valueInteger</code>. The distances between scale points need not be constant.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_SCALE.normal_range</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.referenceRange">Observation.referenceRange</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>archetype</code> scope. <code>DV_SCALE</code> inherits <code>normal_range</code> and <code>other_reference_ranges</code> from <code>DV_ORDERED</code> like every other ordered value, and both share one <code>Observation.referenceRange</code> home and one verdict, which is why they are stated together here. <code>normal_status</code> is <strong>not</strong> folded in with them: its FHIR home and its verdict are different, and it has a row of its own below. Without this row the aggregate would read <code>lossless</code> while a <code>DV_SCALE</code> carrying a <code>normal_range</code> had nowhere stated to put it.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ordered_class">DV_SCALE.normal_status</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.interpretation">Observation.interpretation</a></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ openEHR: drops <code>Observation.interpretation</code> — openEHR binds <code>normal_status</code> to its <code>normal_statuses</code> code system with <code>required</code> strength, so an interpretation coded outside that set cannot be carried; a change request to relax the binding to <code>extensible</code> is open <code>archetype</code> scope. Inherited from <code>DV_ORDERED</code> — § 6.2.1 is where <code>normal_status</code> is declared — and it follows the <code>DV_QUANTITY</code> pattern <strong>including that pattern’s inbound loss</strong>, which is why it is a row of its own rather than a sentence on the reference-range row.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-scale-to-observation-component (generated by npm --prefix reference run render) <<< -->
