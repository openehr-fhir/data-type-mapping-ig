### Coded data

Coded values are the most structurally divergent area between the two type
systems, and they carry the largest share of this guide's open decisions.

openEHR uses a small `CODE_PHRASE` — `terminology_id`, `code_string`, and an
optional `preferred_term` — which `DV_CODED_TEXT` wraps for clinical content and
which several other types use directly to anchor a code: `DV_ORDINAL.symbol`,
`DV_SCALE.symbol`, `DV_STATE.value`, and `null_flavour`. FHIR has `Coding`
(`system`, `version`, `code`, `display`, `userSelected`), `CodeableConcept`
(free text plus zero or more codings), and a `code` primitive for simple closed
value sets.

#### Terminology is shared, even when its representation is not

Most coded mappings have an external source of truth that applies equally in
both models. A LOINC, SNOMED CT, or UCUM code is valid and means the same thing
on either side; only the way the *system* is named differs.

SNOMED CT is the case that most often goes wrong. The HL7 guidance on
[Using SNOMED CT with HL7 Standards](https://terminology.hl7.org/en/SNOMEDCT.html)
describes how `system` identifies the edition and version, and some code systems
embed a version in the URI itself. See
[Cross-Cutting Concerns](cross-cutting.html) for the unresolved question of how
a FHIR `system` plus `version` is combined into a single openEHR
`terminology_id`.

#### Specificity and uniqueness, moving into FHIR

The main concern raised by the terminology group is that **if a `system` is
specified, it must preserve the uniqueness of the meaning the originating system
assigned.**

Consider a facility-local procedure code `PROC123` arriving with no system. A
mapping engine:

- MAY add a `system` when the scope of the issuer is understood and the system
  is unique in that context — for example
  `http://example.org/facility/1111-1111-111-11/local-procedure-codes`;
- SHOULD NOT include a `system` otherwise.

Where a system is added, the engine SHALL ensure it is unique both for the
concept within the facility and for the **meaning** of the code. If another
facility uses `PROC123` for a different procedure, or its mapping to a standard
system could ever differ, it must be a different system URI.

#### Bindings and expectations

Different FHIR elements impose different requirements on cardinality, binding
target, binding strength (`required`, `extensible`, `preferred`, `example`), and
usage context. Implementers SHOULD consult the target element's binding before
deciding how to populate `system`, `code`, and `display`. The same applies in
reverse: openEHR archetypes and templates may constrain coded values to specific
terminologies.

#### Missing `system` or `code`, moving into openEHR

`CODE_PHRASE.terminology_id` and `code_string` are both mandatory. An incoming
FHIR `Coding` may populate neither.

1. If **both** `system` and `code` are absent, `display` SHOULD be mapped to
   `DV_TEXT.value` rather than to `DV_CODED_TEXT`.
2. If **only one** is absent the situation is inherently unsafe. Resolution
   requires local clinical informatics advice; the options are degrading to
   `DV_TEXT`, injecting a default code system where the source is known, or
   treating the value as an exception. **This guide does not choose between
   them** — see [Open Items](open-items.html).

#### Choosing the defining code

When a FHIR `CodeableConcept` carries several codings, the openEHR
`defining_code` is selected in this priority order:

1. the terminology the openEHR template defines for the slot, where it names
   one;
2. the coding marked `userSelected = true`;
3. the first coding in the `coding` array.

The working group has converged on treating FHIR `userSelected` as effectively
equivalent to openEHR `defining_code` for round-tripping purposes, while
recognising that the semantics are not strictly identical.

#### What lives here

`CODE_PHRASE`, `DV_CODED_TEXT`, `TERM_MAPPING`, and the `null_flavour`
correspondences. `DV_ORDINAL` and `DV_SCALE` are coded values but are mapped in
[Quantities](mapping-quantity.html), because their FHIR target is driven by
their ordinal magnitude and both are archetype-scope mappings.

**No ConceptMap resource, local `CodeSystem`, or local `ValueSet` is defined by
this guide.** Terminology stays anchored in
[terminology.hl7.org](https://terminology.hl7.org) or in openEHR's own published
terminology. The correspondence tables here are the input to a later
concept-map pass, not a substitute for one.

See [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:coded (generated by npm --prefix reference run render) >>> -->
| openEHR type | FHIR type | → FHIR | → openEHR | Maturity | Scope |
|-|-|-|-|-|-|
| `CODE_PHRASE` | `Coding` | `lossy` | `lossy` | `open` | `datatype` |
| `DV_CODED_TEXT` | `CodeableConcept \| Coding` | `lossy` | `lossy` | `open` | `datatype` |
| `TERM_MAPPING` | `CodeableConcept.coding` | `lossy` | `lossy` | `open` | `datatype` |
| `NULL_FLAVOUR` | `data-absent-reason` | `lossy` | `lossy` | `open` | `datatype` |
<!-- <<< mapping ledger: summary:coded (generated by npm --prefix reference run render) <<< -->

<a name="code-phrase"></a>

### CODE_PHRASE and Coding

<!-- >>> mapping ledger: mapping:code-phrase-to-coding (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — CODE_PHRASE](https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class) · [FHIR R5 — Coding](https://hl7.org/fhir/R5/datatypes.html#Coding) · [openEHR RM — TERMINOLOGY_ID](https://specifications.openehr.org/releases/RM/latest/support.html#_terminology_identifiers)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [CODE_PHRASE.terminology_id](https://specifications.openehr.org/releases/RM/latest/support.html#_terminology_identifiers) `1..1` | [Coding.system](https://hl7.org/fhir/R5/datatypes.html#Coding) | `lossless` | `lossless` | `open` | The identifier is carried verbatim in both directions. **How a FHIR `system` plus `version` is combined into one `terminology_id` is not decided** — see `Coding.version` below and [Cross-Cutting Concerns](cross-cutting.html). The reference implementation deliberately treats `terminology_id` as opaque, so no candidate format is adopted by accident. |
| [CODE_PHRASE.code_string](https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class) `1..1` | [Coding.code](https://hl7.org/fhir/R5/datatypes.html#Coding) | `lossless` | `lossless` | `open` | The FHIR `code` type forbids leading, trailing, and repeated internal whitespace. openEHR places **no** character restriction on `code_string`, so a code carrying whitespace has no valid FHIR form. A change request proposing the matching restriction on the openEHR side is open. The openEHR attribute is **mandatory** and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces **nothing** rather than inventing a value. See [the mandatory-attribute rule](conventions.html#mandatory-attributes). |
| [CODE_PHRASE.preferred_term](https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class) `0..1` | [Coding.display](https://hl7.org/fhir/R5/datatypes.html#Coding) | `lossless` | `lossless` | `settled` |  |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/support.html#_terminology_identifiers)) | [Coding.version](https://hl7.org/fhir/R5/datatypes.html#Coding) | `unmapped` | `unmapped` | `open` | → FHIR: Nothing is emitted, because the format for splitting a `terminology_id` back into `system` and `version` has not been chosen. (owner: `working-group`) → openEHR: The version is not carried. Three candidate formats for combining it into `terminology_id` are under discussion and **none has been adopted**: `system\|version` (FHIR canonical style), `system (version)` (parenthetical), and `system#version` (FHIR package style). (owner: `working-group`) The reference implementation exposes the join and split as helpers that take the format as a **required parameter with no default**, so no test, render, or build can quietly bless one candidate. Some code systems embed the version in the URI instead, which side-steps the question entirely. |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class)) | [Coding.system[absent]](https://hl7.org/fhir/R5/datatypes.html#Coding) | `unmapped` | `unmapped` | `open` | → FHIR: openEHR always has both, so the absent case never arises in this direction. (owner: `working-group`) → openEHR: An incoming `Coding` may populate neither. If **both** `system` and `code` are absent, `display` SHOULD become `DV_TEXT.value` rather than a `DV_CODED_TEXT`. If **only one** is absent the situation is inherently unsafe and needs local clinical informatics advice; the candidates are degrading to `DV_TEXT`, injecting a default system where the source is known, and treating the value as an exception. **No default strategy is chosen here.** (owner: `working-group`) This row is one of the two **recorded exceptions** to [the mandatory-attribute rule](conventions.html#mandatory-attributes). Because the working group has explicitly refused to decide what an absent `system` should become, the reference implementation substitutes the placeholder `terminology_id` `unknown` **and reports the substitution at `Coding.system[absent]`**, so the conversion is `lossy` rather than silently `lossless`. An absent `Coding.code` gets no such treatment: `code_string` has no defensible placeholder, so nothing is produced at all. `contract.test.ts` pins both behaviours. |
<!-- <<< mapping ledger: mapping:code-phrase-to-coding (generated by npm --prefix reference run render) <<< -->

#### Worked example - a SNOMED CT concept

<!-- >>> mapping ledger: example:code-phrase-to-coding/01-snomed-concept.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "CODE_PHRASE",
  "terminology_id": {
    "value": "http://snomed.info/sct"
  },
  "code_string": "271737000",
  "preferred_term": "Anemia"
}
```
<!-- <<< mapping ledger: example:code-phrase-to-coding/01-snomed-concept.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:code-phrase-to-coding/01-snomed-concept.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "system": "http://snomed.info/sct",
  "code": "271737000",
  "display": "Anemia"
}
```
<!-- <<< mapping ledger: example:code-phrase-to-coding/01-snomed-concept.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_CODED_TEXT and CodeableConcept / Coding

<!-- >>> mapping ledger: mapping:dv-coded-text-to-codeable-concept (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_CODED_TEXT](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class) · [FHIR R5 — CodeableConcept](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept) · [FHIR R5 — Coding](https://hl7.org/fhir/R5/datatypes.html#Coding)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_CODED_TEXT.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class) `1..1` | *when* the value is free text, or differs from the rubric of the defining code: [CodeableConcept.text](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept) <br/>*when* the concept is carried as a bare `Coding` and the value equals the rubric of the defining code in the stated language: [Coding.display](https://hl7.org/fhir/R5/datatypes.html#Coding) | `lossless` | `lossless` | `settled` | `DV_CODED_TEXT.value` is **mandatory**. Where an incoming FHIR value supplies neither `text` nor a `display`, the rubric SHOULD be resolved from the code through a terminology server rather than left empty. |
| [DV_CODED_TEXT.defining_code](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class) `1..1` | [CodeableConcept.coding](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept) | `lossless` | `lossy` | `settled` | → openEHR: drops `CodeableConcept.coding.userSelected` — openEHR has no field marking which coding a user chose; the flag is consumed to select `defining_code` and is not carried any further When several codings are present, `defining_code` is chosen in this order: the terminology the openEHR template defines for the slot; the coding marked `userSelected = true`; the first coding in the array. The working group has converged on treating `userSelected` as effectively equivalent to `defining_code` for round-tripping, while recognising the semantics are not strictly identical. Whether `defining_code` should instead be conveyed by the `coding-purpose` extension is tracked as HTA-170. The openEHR attribute is **mandatory** and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces **nothing** rather than inventing a value. See [the mandatory-attribute rule](conventions.html#mandatory-attributes). |
| [DV_CODED_TEXT.mappings](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class) `0..*` | [CodeableConcept.coding](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept) | `lossy` | `lossless` | `open` | → FHIR: drops `DV_CODED_TEXT.mappings.match` — the degree of equivalence has no element on `Coding`; it is expected to be retrievable from a terminology service through a ConceptMap instead; `DV_CODED_TEXT.mappings.purpose` — no representation has been chosen — see the `TERM_MAPPING.purpose` row — so nothing is emitted rather than a candidate being blessed by default Each additional coding becomes one `TERM_MAPPING`. Because `TERM_MAPPING.match` is mandatory, an incoming coding is given `=`, which is an inference rather than a carried value. See [TERM_MAPPING](#term-mapping) below. A conversion that delegates to `CODE_PHRASE ↔ Coding` **carries that mapping’s drops forward** rather than swallowing them, so a `Coding.version` or an absent `Coding.system` is reported on the composed result too; the rows that declare those drops are on [CODE_PHRASE ↔ Coding](#code-phrase). |
<!-- <<< mapping ledger: mapping:dv-coded-text-to-codeable-concept (generated by npm --prefix reference run render) <<< -->

#### Worked example - a coded diagnosis

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_CODED_TEXT",
  "value": "Anemia",
  "defining_code": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "http://snomed.info/sct"
    },
    "code_string": "271737000",
    "preferred_term": "Anemia"
  }
}
```
<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "coding": [
    {
      "system": "http://snomed.info/sct",
      "code": "271737000",
      "display": "Anemia"
    }
  ],
  "text": "Anemia"
}
```
<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.fhir.json (generated by npm --prefix reference run render) <<< -->

#### Worked example - several codings collapsing into a defining code

A `CodeableConcept` carrying two codings, the second marked `userSelected`. The
flag chooses the `defining_code` and is then consumed - it has no openEHR field to
live in, which is the loss this direction declares.

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "coding": [
    {
      "system": "http://snomed.info/sct",
      "code": "271737000",
      "display": "Anemia"
    },
    {
      "system": "http://hl7.org/fhir/sid/icd-10",
      "code": "D64.9",
      "display": "Anaemia, unspecified",
      "userSelected": true
    }
  ],
  "text": "Anaemia, unspecified"
}
```
<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.fhir.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_CODED_TEXT",
  "value": "Anaemia, unspecified",
  "defining_code": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "http://hl7.org/fhir/sid/icd-10"
    },
    "code_string": "D64.9",
    "preferred_term": "Anaemia, unspecified"
  },
  "mappings": [
    {
      "_type": "TERM_MAPPING",
      "match": "=",
      "target": {
        "_type": "CODE_PHRASE",
        "terminology_id": {
          "value": "http://snomed.info/sct"
        },
        "code_string": "271737000",
        "preferred_term": "Anemia"
      }
    }
  ]
}
```
<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.openehr.json (generated by npm --prefix reference run render) <<< -->

<a name="term-mapping"></a>

### TERM_MAPPING and CodeableConcept.coding

<!-- >>> mapping ledger: mapping:term-mapping-to-coding (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — TERM_MAPPING](https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class) · [FHIR R5 — CodeableConcept](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept) · [FHIR R5 — Coding](https://hl7.org/fhir/R5/datatypes.html#Coding)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [TERM_MAPPING.target](https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class) `1..1` | [Coding.code](https://hl7.org/fhir/R5/datatypes.html#Coding) | `lossless` | `lossless` | `settled` | The mapped term itself, carried exactly as `CODE_PHRASE ↔ Coding` describes. The openEHR attribute is **mandatory** and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces **nothing** rather than inventing a value. See [the mandatory-attribute rule](conventions.html#mandatory-attributes). |
| [TERM_MAPPING.match](https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Coding)) | `unmapped` | `unmapped` | `settled` | → FHIR: The `>`, `=`, `<`, and `?` relationship is not carried in the instance. The working group decided it is retrievable from a terminology service and dropped the request for an instance-level representation. → openEHR: `TERM_MAPPING.match` is mandatory, so a mapping engine must supply a value; `=` is the only defensible default and it is an inference, not a carried value. |
| [TERM_MAPPING.purpose](https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class) `0..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#Coding)) | `unmapped` | `unmapped` | `open` | → FHIR: No representation has been chosen. The candidates are adding the openEHR purpose values to the FHIR `coding-purpose` code system, and using openEHR’s own published `term_mapping_purpose` code system directly. **Neither is adopted here**, so nothing is emitted. (owner: `working-group`) → openEHR: Nothing arrives to map back, and the openEHR IG currently binds `purpose` with `required` strength, which is itself considered incorrect and has an open change request to relax it. (owner: `openehr-modelling`) openEHR publishes the value set at `https://specifications.openehr.org/fhir/valueset-term_mapping_purpose` with the codes *public health*, *reimbursement*, and *research study*. The `alternate-codes` extension has also been proposed, as a way of carrying the whole group of alternate codes on the concept rather than one purpose per coding. |
<!-- <<< mapping ledger: mapping:term-mapping-to-coding (generated by npm --prefix reference run render) <<< -->

#### Worked example - an ICD-10 equivalent

<!-- >>> mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "TERM_MAPPING",
  "match": "=",
  "target": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "http://hl7.org/fhir/sid/icd-10"
    },
    "code_string": "D64.9",
    "preferred_term": "Anaemia, unspecified"
  }
}
```
<!-- <<< mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "system": "http://hl7.org/fhir/sid/icd-10",
  "code": "D64.9",
  "display": "Anaemia, unspecified"
}
```
<!-- <<< mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.fhir.json (generated by npm --prefix reference run render) <<< -->

### null_flavour and data-absent-reason

<!-- >>> mapping ledger: mapping:null-flavour-to-data-absent-reason (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR — Support Terminology](https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html) · [FHIR R5 — missing data and the data-absent-reason extension](https://hl7.org/fhir/R5/extensibility.html#Special-Case) · [THO — data-absent-reason](https://terminology.hl7.org/CodeSystem-data-absent-reason.html) · [THO — v3 NullFlavor](https://terminology.hl7.org/CodeSystem-v3-NullFlavor.html)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [NULL_FLAVOUR.defining_code.terminology_id](https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html) `1..1` | [CodeableConcept.coding.system](https://terminology.hl7.org/CodeSystem-data-absent-reason.html) | `lossless` | `lossless` | `settled` | Fixed on each side: openEHR’s own `openehr` terminology, and `http://terminology.hl7.org/CodeSystem/data-absent-reason`. **No local code system or value set is defined by this guide**; both sides are cited to their own publisher. |
| [NULL_FLAVOUR.defining_code.code_string](https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html) `1..1` | [CodeableConcept.coding.code](https://terminology.hl7.org/CodeSystem-data-absent-reason.html) | `lossy` | `lossy` | `open` | → FHIR: drops `NULL_FLAVOUR.defining_code.code_string` — openEHR `271` (*no information*) has no data-absent-reason equivalent and falls back to `unknown`, which conflates it with `253`; a request to add *no information* to the FHIR code system has not been made → openEHR: drops `CodeableConcept.coding.code` — the data-absent-reason code system is broader than openEHR’s null-flavour set, so codes outside the four mapped parents collapse onto their nearest openEHR ancestor Only L1 codes are mapped explicitly; L2 codes inherit their parent’s mapping. `253` (UNK, *unknown*) ↔ `unknown`, inherited by `asked-unknown`, `temp-unknown`, `not-asked`, `not-a-number`, `negative-infinity`, `positive-infinity`, and `not-performed`. `272` (MSK, *masked*) ↔ `masked`, inherited by `asked-declined` and `not-permitted`. `273` (NA, *not applicable*) ↔ `not-applicable`, inherited by `unsupported`. `271` (NI, *no information*) has no exact counterpart and is the one problematic code. |
| [NULL_FLAVOUR.value](https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html) `1..1` | [CodeableConcept.text](https://hl7.org/fhir/R5/datatypes.html#CodeableConcept) | `lossless` | `lossless` | `settled` | The rubric. `NULL_FLAVOUR.value` is mandatory in openEHR, so where FHIR supplies no `text` it SHOULD be resolved from the code through a terminology server. |
| [ELEMENT.null_flavour](https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html) `0..1` | *when* the absent value is an `Observation.value[x]`: [Observation.dataAbsentReason](https://hl7.org/fhir/R5/observation-definitions.html#Observation.dataAbsentReason) <br/>*when* the absent value is any other element: [Element.extension[data-absent-reason]](https://hl7.org/fhir/extensions/StructureDefinition-data-absent-reason.html)<sup>†</sup> *(extension)* | `lossless` | `lossless` | `settled` | `archetype` scope: which of the two homes applies is decided by the element the value would have occupied, not by the data type. `Observation.dataAbsentReason` is a first-class element; everywhere else the generic extension is used. data-absent-reason is the current guidance, confirmed against national IGs. |
| [ELEMENT.null_flavour](https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html) `0..1` | [Element.extension[iso21090-nullFlavor]](https://hl7.org/fhir/extensions/StructureDefinition-iso21090-nullFlavor.html)<sup>†</sup> *(extension)* | `unmapped` | `lossy` | `open` | → FHIR: data-absent-reason is the current guidance and is what this guide emits. The v3 NullFlavor extension is read on input but not produced on output. (owner: `working-group`) → openEHR: drops `Element.extension[iso21090-nullFlavor]` — the v3 hierarchy is deeper than openEHR’s: `NI` and `INV` (with its children `DER`, `OTH`, `UNC`, `NINF`, `PINF`) all collapse onto `271`, and `UNK` with `ASKU`, `NASK`, `NAVU`, `QS`, `TRC`, and `NAV` all collapse onto `253` `MSK` → `272` and `NA` → `273` are direct. `NP` (*not present*) is retired and message-layer only, and is out of scope. openEHR intends to support both FHIR value sets, with the back-transformation keyed on which code system is present. |
<!-- <<< mapping ledger: mapping:null-flavour-to-data-absent-reason (generated by npm --prefix reference run render) <<< -->

#### Worked example - a masked value

<!-- >>> mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_CODED_TEXT",
  "value": "masked",
  "defining_code": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "openehr"
    },
    "code_string": "272"
  }
}
```
<!-- <<< mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "coding": [
    {
      "system": "http://terminology.hl7.org/CodeSystem/data-absent-reason",
      "code": "masked"
    }
  ],
  "text": "masked"
}
```
<!-- <<< mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.fhir.json (generated by npm --prefix reference run render) <<< -->
