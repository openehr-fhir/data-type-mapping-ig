### Coded data

Coded values are the most structurally divergent area between the two type
systems, and they carry the largest share of this guide's open decisions.

openEHR uses a small `CODE_PHRASE` — `terminology_id`, `code_string`, and an
optional `preferred_term` — which `DV_CODED_TEXT` wraps for clinical content and
which several other types use directly to anchor a code: `DV_ORDINAL.symbol`,
`DV_SCALE.symbol`, `DV_STATE.value`, and `null_flavour`. FHIR has `Coding`
(`system`, `version`, `code`, `display`, `userSelected`), `CodeableConcept`
(free text plus zero or more codings), and a `code` primitive for simple closed
value sets.

#### Terminology is shared, even when its representation is not

Most coded mappings have an external source of truth that applies equally in
both models. A LOINC, SNOMED CT, or UCUM code is valid and means the same thing
on either side; only the way the *system* is named differs.

SNOMED CT is the case that most often goes wrong. The HL7 guidance on
[Using SNOMED CT with HL7 Standards](https://terminology.hl7.org/en/SNOMEDCT.html)
describes how `system` identifies the edition and version, and some code systems
embed a version in the URI itself. See
[Cross-Cutting Concerns](cross-cutting.html) for the unresolved question of how
a FHIR `system` plus `version` is combined into a single openEHR
`terminology_id`.

#### Specificity and uniqueness, moving into FHIR

The main concern raised by the terminology group is that **if a `system` is
specified, it must preserve the uniqueness of the meaning the originating system
assigned.**

Consider a facility-local procedure code `PROC123` arriving with no system. A
mapping engine:

- MAY add a `system` when the scope of the issuer is understood and the system
  is unique in that context — for example
  `http://example.org/facility/1111-1111-111-11/local-procedure-codes`;
- SHOULD NOT include a `system` otherwise.

Where a system is added, the engine SHALL ensure it is unique both for the
concept within the facility and for the **meaning** of the code. If another
facility uses `PROC123` for a different procedure, or its mapping to a standard
system could ever differ, it must be a different system URI.

#### Bindings and expectations

Different FHIR elements impose different requirements on cardinality, binding
target, binding strength (`required`, `extensible`, `preferred`, `example`), and
usage context. Implementers SHOULD consult the target element's binding before
deciding how to populate `system`, `code`, and `display`. The same applies in
reverse: openEHR archetypes and templates may constrain coded values to specific
terminologies.

#### Missing `system` or `code`, moving into openEHR

`CODE_PHRASE.terminology_id` and `code_string` are both mandatory. An incoming
FHIR `Coding` may populate neither.

1. If **both** `system` and `code` are absent, `display` SHOULD be mapped to
   `DV_TEXT.value` rather than to `DV_CODED_TEXT`.
2. If **only one** is absent the situation is inherently unsafe. Resolution
   requires local clinical informatics advice; the options are degrading to
   `DV_TEXT`, injecting a default code system where the source is known, or
   treating the value as an exception. **This guide does not choose between
   them** — see [Open Items](open-items.html).

#### Choosing the defining code

When a FHIR `CodeableConcept` carries several codings, the openEHR
`defining_code` is selected in this priority order:

1. the terminology the openEHR template defines for the slot, where it names
   one;
2. the coding marked `userSelected = true`;
3. the first coding in the `coding` array.

The working group has converged on treating FHIR `userSelected` as effectively
equivalent to openEHR `defining_code` for round-tripping purposes, while
recognising that the semantics are not strictly identical.

#### What lives here

`CODE_PHRASE`, `DV_CODED_TEXT`, `TERM_MAPPING`, and the `null_flavour`
correspondences. `DV_ORDINAL` and `DV_SCALE` are coded values but are mapped in
[Quantities](mapping-quantity.html), because their FHIR target is driven by
their ordinal magnitude and both are archetype-scope mappings.

**No ConceptMap resource, local `CodeSystem`, or local `ValueSet` is defined by
this guide.** Terminology stays anchored in
[terminology.hl7.org](https://terminology.hl7.org) or in openEHR's own published
terminology. The correspondence tables here are the input to a later
concept-map pass, not a substitute for one.

See [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:coded (generated by npm --prefix reference run render) >>> -->

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid">
<thead>
<tr><th>openEHR type</th><th>FHIR type</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Scope</th></tr>
</thead>
<tbody>
<tr><td><code>CODE_PHRASE</code></td><td><code>Coding</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_CODED_TEXT</code></td><td><code>CodeableConcept | Coding</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>TERM_MAPPING</code></td><td><code>CodeableConcept.coding</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>NULL_FLAVOUR</code></td><td><code>data-absent-reason</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: summary:coded (generated by npm --prefix reference run render) <<< -->

<a name="code-phrase"></a>

### CODE_PHRASE and Coding

<!-- >>> mapping ledger: mapping:code-phrase-to-coding (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class">openEHR RM — CODE_PHRASE</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Coding">FHIR R5 — Coding</a> · <a href="https://specifications.openehr.org/releases/RM/latest/support.html#_terminology_identifiers">openEHR RM — TERMINOLOGY_ID</a></p>

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid" id="mapping-code-phrase-to-coding">
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr id="row-code-phrase-to-coding--code-phrase-terminology-id"><td><a href="https://specifications.openehr.org/releases/RM/latest/support.html#_terminology_identifiers">CODE_PHRASE.terminology_id</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Coding">Coding.system</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>open</code></td><td>The identifier is carried verbatim in both directions. <strong>How a FHIR <code>system</code> plus <code>version</code> is combined into one <code>terminology_id</code> is not decided</strong> — see <code>Coding.version</code> below and <a href="cross-cutting.html">Cross-Cutting Concerns</a>. The reference implementation deliberately treats <code>terminology_id</code> as opaque, so no candidate format is adopted by accident.</td></tr>
<tr id="row-code-phrase-to-coding--code-phrase-code-string"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class">CODE_PHRASE.code_string</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Coding">Coding.code</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>open</code></td><td>The FHIR <code>code</code> type forbids leading, trailing, and repeated internal whitespace. openEHR places <strong>no</strong> character restriction on <code>code_string</code>, so a code carrying whitespace has no valid FHIR form. A change request proposing the matching restriction on the openEHR side is open. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr id="row-code-phrase-to-coding--code-phrase-code-string-whitespace"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class">CODE_PHRASE.code_string[whitespace]</a> <code>1..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Coding">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: Nothing is produced: emitting the code anyway would publish an instance no FHIR validator accepts. A change request proposing the matching whitespace restriction on the openEHR side is open. (owner: <code>openehr-modelling</code>) → openEHR: A FHIR <code>code</code> never carries such a value, so the case never arises. (owner: <code>openehr-modelling</code>) A sub-case of <code>CODE_PHRASE.code_string</code>, split out the same way <code>magnitude_status[~]</code> is: the parent row keeps its <code>lossless</code> claim for every code that <em>is</em> a legal FHIR <code>code</code>, and this row states the exception where the round-trip matrix can police it.</td></tr>
<tr id="row-code-phrase-to-coding--code-phrase-preferred-term"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class">CODE_PHRASE.preferred_term</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Coding">Coding.display</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td></td></tr>
<tr id="row-code-phrase-to-coding--fhir-coding-version"><td>—<br/><em>no counterpart</em> (<a href="https://specifications.openehr.org/releases/RM/latest/support.html#_terminology_identifiers">inventory</a>)</td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Coding">Coding.version</a></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: Nothing is emitted, because the format for splitting a <code>terminology_id</code> back into <code>system</code> and <code>version</code> has not been chosen. (owner: <code>working-group</code>) → openEHR: The version is not carried. Three candidate formats for combining it into <code>terminology_id</code> are under discussion and <strong>none has been adopted</strong>: <code>system|version</code> (FHIR canonical style), <code>system (version)</code> (parenthetical), and <code>system#version</code> (FHIR package style). (owner: <code>working-group</code>) The reference implementation exposes the join and split as helpers that take the format as a <strong>required parameter with no default</strong>, so no test, render, or build can quietly bless one candidate. Some code systems embed the version in the URI instead, which side-steps the question entirely.</td></tr>
<tr id="row-code-phrase-to-coding--fhir-coding-system-absent"><td>—<br/><em>no counterpart</em> (<a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class">inventory</a>)</td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Coding">Coding.system[absent]</a></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: openEHR always has both, so the absent case never arises in this direction. (owner: <code>working-group</code>) → openEHR: An incoming <code>Coding</code> may populate neither. If <strong>both</strong> <code>system</code> and <code>code</code> are absent, <code>display</code> SHOULD become <code>DV_TEXT.value</code> rather than a <code>DV_CODED_TEXT</code>. If <strong>only one</strong> is absent the situation is inherently unsafe and needs local clinical informatics advice; the candidates are degrading to <code>DV_TEXT</code>, injecting a default system where the source is known, and treating the value as an exception. <strong>No default strategy is chosen here.</strong> (owner: <code>working-group</code>) This row is one of the two <strong>recorded exceptions</strong> to <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>. Because the working group has explicitly refused to decide what an absent <code>system</code> should become, the reference implementation substitutes the placeholder <code>terminology_id</code> <code>unknown</code> <strong>and reports the substitution at <code>Coding.system[absent]</code></strong>, so the conversion is <code>lossy</code> rather than silently <code>lossless</code>. An absent <code>Coding.code</code> gets no such treatment: <code>code_string</code> has no defensible placeholder, so nothing is produced at all. <code>contract.test.ts</code> pins both behaviours.</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: mapping:code-phrase-to-coding (generated by npm --prefix reference run render) <<< -->

#### Worked example - a SNOMED CT concept

<!-- >>> mapping ledger: example:code-phrase-to-coding/01-snomed-concept.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "CODE_PHRASE",
  "terminology_id": {
    "value": "http://snomed.info/sct"
  },
  "code_string": "271737000",
  "preferred_term": "Anemia"
}
```

<!-- <<< mapping ledger: example:code-phrase-to-coding/01-snomed-concept.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:code-phrase-to-coding/01-snomed-concept.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "system": "http://snomed.info/sct",
  "code": "271737000",
  "display": "Anemia"
}
```

<!-- <<< mapping ledger: example:code-phrase-to-coding/01-snomed-concept.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_CODED_TEXT and CodeableConcept / Coding

<!-- >>> mapping ledger: mapping:dv-coded-text-to-codeable-concept (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class">openEHR RM — DV_CODED_TEXT</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#CodeableConcept">FHIR R5 — CodeableConcept</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Coding">FHIR R5 — Coding</a></p>

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid" id="mapping-dv-coded-text-to-codeable-concept">
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr id="row-dv-coded-text-to-codeable-concept--dv-coded-text-value"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class">DV_CODED_TEXT.value</a> <code>1..1</code></td><td><em>when</em> the value is free text, or differs from the rubric of the defining code: <a href="https://hl7.org/fhir/R5/datatypes.html#CodeableConcept">CodeableConcept.text</a><br/><em>when</em> the concept is carried as a bare <code>Coding</code> and the value equals the rubric of the defining code in the stated language: <a href="https://hl7.org/fhir/R5/datatypes.html#Coding">Coding.display</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>DV_CODED_TEXT.value</code> is <strong>mandatory</strong>. Where an incoming FHIR value supplies neither <code>text</code> nor a <code>display</code>, the rubric SHOULD be resolved from the code through a terminology server rather than left empty.</td></tr>
<tr id="row-dv-coded-text-to-codeable-concept--dv-coded-text-defining-code"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class">DV_CODED_TEXT.defining_code</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#CodeableConcept">CodeableConcept.coding</a></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>settled</code></td><td>→ openEHR: drops <code>CodeableConcept.coding.userSelected</code> — openEHR has no field marking which coding a user chose; the flag is consumed to select <code>defining_code</code> and is not carried any further; <code>Coding.version</code> — openEHR has no separate version field and no format for combining system and version into <code>terminology_id</code> has been chosen, so the version is not carried — this conversion delegates to <code>CODE_PHRASE ↔ Coding</code> and carries that mapping’s drop forward rather than swallowing it When several codings are present, <code>defining_code</code> is chosen in this order: the terminology the openEHR template defines for the slot; the coding marked <code>userSelected = true</code>; the first coding in the array. The working group has converged on treating <code>userSelected</code> as effectively equivalent to <code>defining_code</code> for round-tripping, while recognising the semantics are not strictly identical. Whether <code>defining_code</code> should instead be conveyed by the <code>coding-purpose</code> extension is tracked as HTA-170. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr id="row-dv-coded-text-to-codeable-concept--dv-coded-text-mappings"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_coded_text_class">DV_CODED_TEXT.mappings</a> <code>0..*</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#CodeableConcept">CodeableConcept.coding</a></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ FHIR: drops <code>DV_CODED_TEXT.mappings.match</code> — the degree of equivalence has no element on <code>Coding</code>; it is expected to be retrievable from a terminology service through a ConceptMap instead; <code>DV_CODED_TEXT.mappings.purpose</code> — no representation has been chosen — see the <code>TERM_MAPPING.purpose</code> row — so nothing is emitted rather than a candidate being blessed by default → openEHR: drops <code>TERM_MAPPING.match</code> — each additional coding becomes one <code>TERM_MAPPING</code>, whose mandatory <code>match</code> a <code>Coding</code> cannot source. The composed conversion carries the inner drop forward: <code>?</code> — the Reference Model’s <em>"the kind of mapping is unknown"</em> — is written and reported on this result too. See the <a href="#term-mapping">TERM_MAPPING ↔ Coding</a> rows Each additional coding becomes one <code>TERM_MAPPING</code>, produced by the registered <code>TERM_MAPPING ↔ Coding</code> converter rather than by a second copy of it. Because <code>TERM_MAPPING.match</code> is mandatory and a <code>Coding</code> has no degree of equivalence to source it from, an incoming coding is given the Reference Model’s own <code>?</code> — <em>"the kind of mapping is unknown"</em> — <strong>and the substitution is reported</strong>. See <a href="#term-mapping">TERM_MAPPING</a> below. A conversion that delegates to <code>CODE_PHRASE ↔ Coding</code> <strong>carries that mapping’s drops forward</strong> rather than swallowing them, so a <code>Coding.version</code> or an absent <code>Coding.system</code> is reported on the composed result too; the rows that declare those drops are on <a href="#code-phrase">CODE_PHRASE ↔ Coding</a>.</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: mapping:dv-coded-text-to-codeable-concept (generated by npm --prefix reference run render) <<< -->

#### Worked example - a coded diagnosis

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_CODED_TEXT",
  "value": "Anemia",
  "defining_code": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "http://snomed.info/sct"
    },
    "code_string": "271737000",
    "preferred_term": "Anemia"
  }
}
```

<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "coding": [
    {
      "system": "http://snomed.info/sct",
      "code": "271737000",
      "display": "Anemia"
    }
  ],
  "text": "Anemia"
}
```

<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/01-simple-concept.fhir.json (generated by npm --prefix reference run render) <<< -->

#### Worked example - several codings collapsing into a defining code

A `CodeableConcept` carrying two codings, the second marked `userSelected`. The
flag chooses the `defining_code` and is then consumed - it has no openEHR field to
live in, which is the loss this direction declares.

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "coding": [
    {
      "system": "http://snomed.info/sct",
      "code": "271737000",
      "display": "Anemia"
    },
    {
      "system": "http://hl7.org/fhir/sid/icd-10",
      "code": "D64.9",
      "display": "Anaemia, unspecified",
      "userSelected": true
    }
  ],
  "text": "Anaemia, unspecified"
}
```

<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.fhir.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_CODED_TEXT",
  "value": "Anaemia, unspecified",
  "defining_code": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "http://hl7.org/fhir/sid/icd-10"
    },
    "code_string": "D64.9",
    "preferred_term": "Anaemia, unspecified"
  },
  "mappings": [
    {
      "_type": "TERM_MAPPING",
      "match": "?",
      "target": {
        "_type": "CODE_PHRASE",
        "terminology_id": {
          "value": "http://snomed.info/sct"
        },
        "code_string": "271737000",
        "preferred_term": "Anemia"
      }
    }
  ]
}
```

<!-- <<< mapping ledger: example:dv-coded-text-to-codeable-concept/03-user-selected.openehr.json (generated by npm --prefix reference run render) <<< -->

<a name="term-mapping"></a>

### TERM_MAPPING and CodeableConcept.coding

<!-- >>> mapping ledger: mapping:term-mapping-to-coding (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class">openEHR RM — TERM_MAPPING</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#CodeableConcept">FHIR R5 — CodeableConcept</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#Coding">FHIR R5 — Coding</a></p>

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid" id="mapping-term-mapping-to-coding">
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr id="row-term-mapping-to-coding--term-mapping-target"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class">TERM_MAPPING.target</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#Coding">Coding.code</a></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>settled</code></td><td>→ openEHR: drops <code>Coding.version</code> — openEHR has no separate version field and no format for combining system and version into <code>terminology_id</code> has been chosen, so the version is not carried — this conversion delegates to <code>CODE_PHRASE ↔ Coding</code> and carries that mapping’s drop forward rather than swallowing it The mapped term itself, carried exactly as <code>CODE_PHRASE ↔ Coding</code> describes. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr id="row-term-mapping-to-coding--term-mapping-match"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class">TERM_MAPPING.match</a> <code>1..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Coding">inventory</a>)</td><td><code>unmapped</code></td><td><code>lossy</code></td><td><code>settled</code></td><td>→ FHIR: The <code>&gt;</code>, <code>=</code>, <code>&lt;</code>, and <code>?</code> relationship is not carried in the instance. The working group decided it is retrievable from a terminology service and dropped the request for an instance-level representation. → openEHR: drops <code>TERM_MAPPING.match</code> — <code>Coding</code> carries no degree of equivalence, and <code>TERM_MAPPING.match</code> is <code>1..1</code>. The Reference Model publishes a value for exactly this case — <code>?</code>, <em>"the kind of mapping is unknown"</em> — so <code>?</code> is written <strong>and reported</strong>, and no equivalence is asserted. Substituting <code>=</code> would claim the two terms are equivalent on no evidence, which the <a href="conventions.html#mandatory-attributes">mandatory-attribute rule</a> forbids The substitution is the <strong>third and last</strong> recorded exception to the mandatory-attribute rule, and it is on the published closed list. It is not a default: <code>?</code> is the RM’s designated unknown, so writing it asserts nothing about the two terms, and the conversion is <code>lossy</code> rather than <code>lossless</code> because the value came from the mapping engine and not from the source.</td></tr>
<tr id="row-term-mapping-to-coding--term-mapping-purpose"><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_term_mapping_class">TERM_MAPPING.purpose</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#Coding">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: No representation has been chosen. The candidates are adding the openEHR purpose values to the FHIR <code>coding-purpose</code> code system, and using openEHR’s own published <code>term_mapping_purpose</code> code system directly. <strong>Neither is adopted here</strong>, so nothing is emitted. (owner: <code>working-group</code>) → openEHR: Nothing arrives to map back, and the openEHR IG currently binds <code>purpose</code> with <code>required</code> strength, which is itself considered incorrect and has an open change request to relax it. (owner: <code>openehr-modelling</code>) openEHR publishes the value set at <code>https://specifications.openehr.org/fhir/valueset-term_mapping_purpose</code> with the codes <em>public health</em>, <em>reimbursement</em>, and <em>research study</em>. The <code>alternate-codes</code> extension has also been proposed, as a way of carrying the whole group of alternate codes on the concept rather than one purpose per coding.</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: mapping:term-mapping-to-coding (generated by npm --prefix reference run render) <<< -->

#### Worked example - an ICD-10 equivalent

<!-- >>> mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "TERM_MAPPING",
  "match": "=",
  "target": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "http://hl7.org/fhir/sid/icd-10"
    },
    "code_string": "D64.9",
    "preferred_term": "Anaemia, unspecified"
  }
}
```

<!-- <<< mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "system": "http://hl7.org/fhir/sid/icd-10",
  "code": "D64.9",
  "display": "Anaemia, unspecified"
}
```

<!-- <<< mapping ledger: example:term-mapping-to-coding/01-icd10-equivalent.fhir.json (generated by npm --prefix reference run render) <<< -->

### null_flavour and data-absent-reason

<!-- >>> mapping ledger: mapping:null-flavour-to-data-absent-reason (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html#_null_flavours">openEHR — Support Terminology, null flavours</a> · <a href="https://hl7.org/fhir/R5/extensibility.html#Special-Case">FHIR R5 — missing data and the data-absent-reason extension</a> · <a href="https://terminology.hl7.org/CodeSystem-data-absent-reason.html">THO — data-absent-reason</a> · <a href="https://terminology.hl7.org/CodeSystem-v3-NullFlavor.html">THO — v3 NullFlavor</a></p>

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid" id="mapping-null-flavour-to-data-absent-reason">
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr id="row-null-flavour-to-data-absent-reason--null-flavour-terminology-id"><td><a href="https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html#_null_flavours">NULL_FLAVOUR.defining_code.terminology_id</a> <code>1..1</code></td><td><a href="https://terminology.hl7.org/CodeSystem-data-absent-reason.html">CodeableConcept.coding.system</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>Fixed on each side: openEHR’s own <code>openehr</code> terminology, and <code>http://terminology.hl7.org/CodeSystem/data-absent-reason</code>. <strong>No local code system or value set is defined by this guide</strong>; both sides are cited to their own publisher.</td></tr>
<tr id="row-null-flavour-to-data-absent-reason--null-flavour-code-string"><td><a href="https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html#_null_flavours">NULL_FLAVOUR.defining_code.code_string</a> <code>1..1</code></td><td><a href="https://terminology.hl7.org/CodeSystem-data-absent-reason.html">CodeableConcept.coding.code</a></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ FHIR: drops <code>NULL_FLAVOUR.defining_code.code_string</code> — openEHR <code>271</code> (<em>no information</em>) has no data-absent-reason equivalent and falls back to <code>unknown</code>, which conflates it with <code>253</code>; a request to add <em>no information</em> to the FHIR code system has not been made → openEHR: drops <code>CodeableConcept.coding.code</code> — the data-absent-reason code system is broader than openEHR’s null-flavour set, so codes outside the four mapped parents collapse onto their nearest openEHR ancestor Only L1 codes are mapped explicitly; L2 codes inherit their parent’s mapping. <code>253</code> (UNK, <em>unknown</em>) ↔ <code>unknown</code>, inherited by <code>asked-unknown</code>, <code>temp-unknown</code>, <code>not-asked</code>, <code>not-a-number</code>, <code>negative-infinity</code>, <code>positive-infinity</code>, and <code>not-performed</code>. <code>272</code> (MSK, <em>masked</em>) ↔ <code>masked</code>, inherited by <code>asked-declined</code> and <code>not-permitted</code>. <code>273</code> (NA, <em>not applicable</em>) ↔ <code>not-applicable</code>, inherited by <code>unsupported</code>. <code>271</code> (NI, <em>no information</em>) has no exact counterpart and is the one problematic code.</td></tr>
<tr id="row-null-flavour-to-data-absent-reason--null-flavour-value"><td><a href="https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html#_null_flavours">NULL_FLAVOUR.value</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#CodeableConcept">CodeableConcept.text</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>The rubric. <code>NULL_FLAVOUR.value</code> is mandatory in openEHR, so where FHIR supplies no <code>text</code> it SHOULD be resolved from the code through a terminology server.</td></tr>
<tr id="row-null-flavour-to-data-absent-reason--null-flavour-observation-data-absent-reason"><td><a href="https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html#_null_flavours">ELEMENT.null_flavour</a> <code>0..1</code></td><td><em>when</em> the absent value is an <code>Observation.value[x]</code>: <a href="https://hl7.org/fhir/R5/observation-definitions.html#Observation.dataAbsentReason">Observation.dataAbsentReason</a><br/><em>when</em> the absent value is any other element: <a href="https://hl7.org/fhir/extensions/StructureDefinition-data-absent-reason.html">Element.extension[data-absent-reason]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>archetype</code> scope: which of the two homes applies is decided by the element the value would have occupied, not by the data type. <code>Observation.dataAbsentReason</code> is a first-class element; everywhere else the generic extension is used. data-absent-reason is the current guidance, confirmed against national IGs.</td></tr>
<tr id="row-null-flavour-to-data-absent-reason--fhir-iso21090-nullflavor"><td><a href="https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html#_null_flavours">ELEMENT.null_flavour</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-iso21090-nullFlavor.html">Element.extension[iso21090-nullFlavor]</a><sup>†</sup> <em>(extension)</em></td><td><code>unmapped</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ FHIR: data-absent-reason is the current guidance and is what this guide emits. The v3 NullFlavor extension is read on input but not produced on output. (owner: <code>working-group</code>) → openEHR: drops <code>Element.extension[iso21090-nullFlavor]</code> — the v3 hierarchy is deeper than openEHR’s: <code>NI</code> and <code>INV</code> (with its children <code>DER</code>, <code>OTH</code>, <code>UNC</code>, <code>NINF</code>, <code>PINF</code>) all collapse onto <code>271</code>, and <code>UNK</code> with <code>ASKU</code>, <code>NASK</code>, <code>NAVU</code>, <code>QS</code>, <code>TRC</code>, and <code>NAV</code> all collapse onto <code>253</code> <code>MSK</code> → <code>272</code> and <code>NA</code> → <code>273</code> are direct. <code>NP</code> (<em>not present</em>) is retired and message-layer only, and is out of scope. openEHR intends to support both FHIR value sets, with the back-transformation keyed on which code system is present.</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: mapping:null-flavour-to-data-absent-reason (generated by npm --prefix reference run render) <<< -->

#### Worked example - a masked value

<!-- >>> mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_CODED_TEXT",
  "value": "masked",
  "defining_code": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "openehr"
    },
    "code_string": "272"
  }
}
```

<!-- <<< mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "coding": [
    {
      "system": "http://terminology.hl7.org/CodeSystem/data-absent-reason",
      "code": "masked"
    }
  ],
  "text": "masked"
}
```

<!-- <<< mapping ledger: example:null-flavour-to-data-absent-reason/01-masked.fhir.json (generated by npm --prefix reference run render) <<< -->
