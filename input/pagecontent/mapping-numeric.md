### Numeric primitives

openEHR's Foundation Types specification defines primitive value classes —
`Integer`, `Integer64`, `Real`, `Double` — that carry a raw value plus a set of
associated functions. These are **generally not used directly in archetypes**.
Numbers are normally carried inside `DV_` wrappers (`DV_COUNT`, `DV_QUANTITY`,
`DV_PROPORTION`, `DV_ORDINAL`, `DV_SCALE`), and the Foundation primitives appear
as the declared types of *attributes* of those classes.

FHIR takes the opposite shape. It defines stand-alone numeric primitives —
`integer`, `integer64`, `decimal`, `positiveInt`, `unsignedInt` — that are used
directly, both inside complex types and as resource elements.

The consequence is that this category is mostly consulted **through** another
one: a mapping for `DV_QUANTITY.magnitude` is a `Real` → `decimal` mapping, and
the rules below are what make it well defined.

Four facts govern every row here.

- **FHIR `decimal` preserves lexical precision; openEHR `Real` and `Double` do
  not.** FHIR treats trailing zeros in the lexical form as significant. Where
  precision must survive a round trip, either write the value with explicit
  trailing zeros or carry it in the `quantity-precision` extension.
- **FHIR `integer` is 32-bit.** FHIR defines `integer64` for values that may
  exceed 2³¹ − 1, and serialises it as a **JSON String** rather than a JSON
  number, "due to issues with precision in floating point libraries". A guide
  that publishes a bare number for an `integer64` publishes a wire format R5
  does not have.
- **`integer64` is rare in practice.** Most FHIR elements holding integer data
  are typed `integer`. For a 64-bit openEHR attribute it is generally more
  useful to map in-range values into the designated 32-bit element and use an
  extension for anything that exceeds it, than to expect an `integer64` element
  to be there.
- **`Real` → `decimal` needs care.** Binary floating-point values do not always
  have an exact decimal lexical form, and a naive conversion can widen or narrow
  the apparent precision of a clinical value.

See [Quantities](mapping-quantity.html) for the wrappers these primitives are
carried in, and [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:numeric (generated by npm --prefix reference run render) >>> -->

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid">
<thead>
<tr><th>openEHR type</th><th>FHIR type</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Scope</th></tr>
</thead>
<tbody>
<tr><td><code>Integer</code></td><td><code>integer</code></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td><code>datatype</code></td></tr>
<tr><td><code>Integer64</code></td><td><code>integer64</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>Real / Double</code></td><td><code>decimal</code></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>settled</code></td><td><code>datatype</code></td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: summary:numeric (generated by npm --prefix reference run render) <<< -->

### Integer and integer

<!-- >>> mapping ledger: mapping:integer-to-integer (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer_class">openEHR BASE — Integer</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#integer">FHIR R5 — integer</a></p>

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid" id="mapping-integer-to-integer">
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr id="row-integer-to-integer--integer-value"><td><a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer_class">Integer</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#integer">integer</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>Both are 32-bit signed integers, so the ranges coincide. openEHR Foundation primitives are generally not used directly in archetypes: numbers are carried inside <code>DV_</code> wrappers, and this row states what the wrapper’s attribute mapping means.</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: mapping:integer-to-integer (generated by npm --prefix reference run render) <<< -->

### Integer64 and integer64

<!-- >>> mapping ledger: mapping:integer64-to-integer64 (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer64_class">openEHR BASE — Integer64</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#integer64">FHIR R5 — integer64</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#integer">FHIR R5 — integer</a></p>

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid" id="mapping-integer64-to-integer64">
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr id="row-integer64-to-integer64--integer64-value"><td><a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer64_class">Integer64</a> <code>1..1</code></td><td><em>when</em> the target element is typed <code>integer64</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#integer64">integer64</a><br/><em>when</em> the target element is typed <code>integer</code> and the value is within the 32-bit range: <a href="https://hl7.org/fhir/R5/datatypes.html#integer">integer</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>FHIR serialises <code>integer64</code> as a <strong>JSON String</strong>, not as a JSON number, "due to issues with precision in floating point libraries" — a guide that publishes a bare number for one publishes a wire format R5 does not have. <code>integer64</code> is also <strong>rare in practice</strong>: most FHIR elements holding integer data are typed <code>integer</code>. For a 64-bit openEHR attribute it is generally more useful to map in-range values into the designated 32-bit element and use an extension for anything that exceeds it, than to expect an <code>integer64</code> element to be there — which is why the row below, and the reference implementation, treat the 32-bit range as the one that decides whether a value can be carried.</td></tr>
<tr id="row-integer64-to-integer64--integer64-overflow"><td><a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer64_class">Integer64[overflow]</a> <code>1..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#integer">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>open</code></td><td>→ FHIR: A value exceeding 2³¹ − 1 cannot be carried in an element typed <code>integer</code>. An extension is required, and which extension depends on the element, so no general answer is given here. (owner: <code>working-group</code>) → openEHR: Nothing arrives, because nothing was emitted. (owner: <code>working-group</code>)</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: mapping:integer64-to-integer64 (generated by npm --prefix reference run render) <<< -->

### Real / Double and decimal

<!-- >>> mapping ledger: mapping:real-to-decimal (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_real_class">openEHR BASE — Real</a> · <a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_double_class">openEHR BASE — Double</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#decimal">FHIR R5 — decimal</a></p>

<div style="max-width: 100%; overflow-x: auto;" tabindex="0" role="group" aria-label="Scrollable table">
<table class="grid" id="mapping-real-to-decimal">
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th style="white-space: nowrap;">→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr id="row-real-to-decimal--real-value"><td><a href="https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_real_class">Real</a> <code>1..1</code></td><td><a href="https://hl7.org/fhir/R5/datatypes.html#decimal">decimal</a></td><td><code>lossless</code></td><td><code>lossy</code></td><td><code>settled</code></td><td>→ openEHR: drops <code>decimal</code> — FHIR treats trailing zeros in the lexical form of a <code>decimal</code> as significant; openEHR <code>Real</code> and <code>Double</code> do not carry lexical precision at all, so the significance is lost unless a receiving <code>DV_QUANTITY.precision</code> is available to take it Where precision must survive a round trip, either write the value with explicit trailing zeros or carry it in the <code>quantity-precision</code> extension; see <a href="mapping-quantity.html">Quantities</a>. <code>Double</code> maps to <code>decimal</code> on the same terms as <code>Real</code>. Care is needed converting binary floating-point values, whose exact decimal lexical form can widen or narrow the apparent precision of a clinical value.</td></tr>
</tbody>
</table>
</div>

<!-- <<< mapping ledger: mapping:real-to-decimal (generated by npm --prefix reference run render) <<< -->
