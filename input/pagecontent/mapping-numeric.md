### Numeric primitives

openEHR's Foundation Types specification defines primitive value classes —
`Integer`, `Integer64`, `Real`, `Double` — that carry a raw value plus a set of
associated functions. These are **generally not used directly in archetypes**.
Numbers are normally carried inside `DV_` wrappers (`DV_COUNT`, `DV_QUANTITY`,
`DV_PROPORTION`, `DV_ORDINAL`, `DV_SCALE`), and the Foundation primitives appear
as the declared types of *attributes* of those classes.

FHIR takes the opposite shape. It defines stand-alone numeric primitives —
`integer`, `integer64`, `decimal`, `positiveInt`, `unsignedInt` — that are used
directly, both inside complex types and as resource elements.

The consequence is that this category is mostly consulted **through** another
one: a mapping for `DV_QUANTITY.magnitude` is a `Real` → `decimal` mapping, and
the rules below are what make it well defined.

Four facts govern every row here.

- **FHIR `decimal` preserves lexical precision; openEHR `Real` and `Double` do
  not.** FHIR treats trailing zeros in the lexical form as significant. Where
  precision must survive a round trip, either write the value with explicit
  trailing zeros or carry it in the `quantity-precision` extension.
- **FHIR `integer` is 32-bit.** FHIR defines `integer64` for values that may
  exceed 2³¹ − 1, and serialises it as a **JSON String** rather than a JSON
  number, "due to issues with precision in floating point libraries". A guide
  that publishes a bare number for an `integer64` publishes a wire format R5
  does not have.
- **`integer64` is rare in practice.** Most FHIR elements holding integer data
  are typed `integer`. For a 64-bit openEHR attribute it is generally more
  useful to map in-range values into the designated 32-bit element and use an
  extension for anything that exceeds it, than to expect an `integer64` element
  to be there.
- **`Real` → `decimal` needs care.** Binary floating-point values do not always
  have an exact decimal lexical form, and a naive conversion can widen or narrow
  the apparent precision of a clinical value.

See [Quantities](mapping-quantity.html) for the wrappers these primitives are
carried in, and [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:numeric (generated by npm --prefix reference run render) >>> -->

| openEHR type | FHIR type | → FHIR | → openEHR | Maturity | Scope |
|-|-|-|-|-|-|
| `Integer` | `integer` | `lossless` | `lossless` | `settled` | `datatype` |
| `Integer64` | `integer64` | `lossy` | `lossy` | `open` | `datatype` |
| `Real / Double` | `decimal` | `lossless` | `lossy` | `settled` | `datatype` |

<!-- <<< mapping ledger: summary:numeric (generated by npm --prefix reference run render) <<< -->

### Integer and integer

<!-- >>> mapping ledger: mapping:integer-to-integer (generated by npm --prefix reference run render) >>> -->

**Sources:** [openEHR BASE — Integer](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer_class) · [FHIR R5 — integer](https://hl7.org/fhir/R5/datatypes.html#integer)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [Integer](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer_class) `1..1` | [integer](https://hl7.org/fhir/R5/datatypes.html#integer) | `lossless` | `lossless` | `settled` | Both are 32-bit signed integers, so the ranges coincide. openEHR Foundation primitives are generally not used directly in archetypes: numbers are carried inside `DV_` wrappers, and this row states what the wrapper’s attribute mapping means. |

<!-- <<< mapping ledger: mapping:integer-to-integer (generated by npm --prefix reference run render) <<< -->

### Integer64 and integer64

<!-- >>> mapping ledger: mapping:integer64-to-integer64 (generated by npm --prefix reference run render) >>> -->

**Sources:** [openEHR BASE — Integer64](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer64_class) · [FHIR R5 — integer64](https://hl7.org/fhir/R5/datatypes.html#integer64) · [FHIR R5 — integer](https://hl7.org/fhir/R5/datatypes.html#integer)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [Integer64](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer64_class) `1..1` | *when* the target element is typed `integer64`: [integer64](https://hl7.org/fhir/R5/datatypes.html#integer64) <br/>*when* the target element is typed `integer` and the value is within the 32-bit range: [integer](https://hl7.org/fhir/R5/datatypes.html#integer) | `lossless` | `lossless` | `settled` | FHIR serialises `integer64` as a **JSON String**, not as a JSON number, "due to issues with precision in floating point libraries" — a guide that publishes a bare number for one publishes a wire format R5 does not have. `integer64` is also **rare in practice**: most FHIR elements holding integer data are typed `integer`. For a 64-bit openEHR attribute it is generally more useful to map in-range values into the designated 32-bit element and use an extension for anything that exceeds it, than to expect an `integer64` element to be there — which is why the row below, and the reference implementation, treat the 32-bit range as the one that decides whether a value can be carried. |
| [Integer64[overflow]](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_integer64_class) `1..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/datatypes.html#integer)) | `unmapped` | `unmapped` | `open` | → FHIR: A value exceeding 2³¹ − 1 cannot be carried in an element typed `integer`. An extension is required, and which extension depends on the element, so no general answer is given here. (owner: `working-group`) → openEHR: Nothing arrives, because nothing was emitted. (owner: `working-group`) |

<!-- <<< mapping ledger: mapping:integer64-to-integer64 (generated by npm --prefix reference run render) <<< -->

### Real / Double and decimal

<!-- >>> mapping ledger: mapping:real-to-decimal (generated by npm --prefix reference run render) >>> -->

**Sources:** [openEHR BASE — Real](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_real_class) · [openEHR BASE — Double](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_double_class) · [FHIR R5 — decimal](https://hl7.org/fhir/R5/datatypes.html#decimal)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [Real](https://specifications.openehr.org/releases/BASE/latest/foundation_types.html#_real_class) `1..1` | [decimal](https://hl7.org/fhir/R5/datatypes.html#decimal) | `lossless` | `lossy` | `settled` | → openEHR: drops `decimal` — FHIR treats trailing zeros in the lexical form of a `decimal` as significant; openEHR `Real` and `Double` do not carry lexical precision at all, so the significance is lost unless a receiving `DV_QUANTITY.precision` is available to take it Where precision must survive a round trip, either write the value with explicit trailing zeros or carry it in the `quantity-precision` extension; see [Quantities](mapping-quantity.html). `Double` maps to `decimal` on the same terms as `Real`. Care is needed converting binary floating-point values, whose exact decimal lexical form can widen or narrow the apparent precision of a clinical value. |

<!-- <<< mapping ledger: mapping:real-to-decimal (generated by npm --prefix reference run render) <<< -->
