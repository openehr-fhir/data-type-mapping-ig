### Resource-locator data and references

This category covers the openEHR types that carry a pointer to *something else*
— an opaque URI (`DV_URI`, `DV_EHR_URI`) or a structured link to another node in
an EHR or to an external party (`LINK`, `PARTY_IDENTIFIED`) — and the FHIR types
that play the same role: the URI primitives (`uri`, `url`, `oid`, `uuid`,
`canonical`) and the structured `Reference` and `CodeableReference` data types.
`DV_IDENTIFIER` ↔ `Identifier` is also here, because an identifier is a pointer
at a thing rather than a description of one.

Five facts shape this category.

**The FHIR URI target depends on the element, not on the openEHR type.** FHIR
provides several URI primitives with progressively narrower constraints
(`uri`/`canonical`, `url`, `oid`/`uuid`), and `canonical` additionally carries
versioning semantics. Which one a `DV_URI` maps to is decided by the *element
definition* the value is being carried in.

**The `ehr:` scheme has no FHIR counterpart.** `DV_EHR_URI` constrains the
scheme to `ehr:` and addresses items inside an openEHR EHR — compositions,
sections, entries, or sub-elements. No standard FHIR scheme conveys equivalent
semantics. The closest analogues are absolute or relative `Reference` values,
which address a *resource*, not an element within one.

**FHIR does not natively address sub-elements.** A `LINK.target` can point at a
sub-element of a composition. `Reference.reference` is a resource-level pointer,
with `#fragment` reserved for contained resources. Two standard extensions cover
sub-element addressing when it is genuinely required — `targetElement`, carrying
a target `Element.id`, and `targetPath`, carrying a restricted FHIRPath
expression — but most consumers do not interpret them, so a mapping into FHIR
should avoid producing them unless the source data really needs that
granularity.

**openEHR splits what FHIR joins.** openEHR separates internal record links
(`LINK`) from external party references (`PARTY_IDENTIFIED`); FHIR uses
`Reference` for both. The working group is investigating consolidating `LINK`
and `PARTY_IDENTIFIED` onto a common RM class, likely based on `OBJECT_REF`.
**This guide maps the Reference Model as published today** and records the
pending change as an open item rather than anticipating it.

**FHIR joins what openEHR splits.** `CodeableReference` combines a coded concept
*and* a reference in one value. openEHR has no direct equivalent; mapping one
into openEHR generally means splitting it into a coded element plus a `LINK`, or
into an enclosing `CLUSTER`.

See [Coded Data](mapping-coded.html) for the coded half of a
`CodeableReference`, [Gaps](gaps.html) for the FHIR elements with no openEHR
home, and [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:reference (generated by npm --prefix reference run render) >>> -->
| openEHR type | FHIR type | → FHIR | → openEHR | Maturity | Scope |
|-|-|-|-|-|-|
| `DV_IDENTIFIER` | `Identifier` | `lossy` | `lossy` | `open` | `datatype` |
| `DV_URI / DV_EHR_URI` | `uri \| url` | `lossless` | `lossy` | `open` | `datatype` |
| `LINK` | `Reference \| CodeableReference` | `lossy` | `lossy` | `open` | `datatype` |
<!-- <<< mapping ledger: summary:reference (generated by npm --prefix reference run render) <<< -->

### DV_IDENTIFIER and Identifier

<!-- >>> mapping ledger: mapping:dv-identifier-to-identifier (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_IDENTIFIER](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_identifier_class) · [FHIR R5 — Identifier](https://hl7.org/fhir/R5/datatypes.html#Identifier)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_IDENTIFIER.id](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_identifier_class) `1..1` | [Identifier.value](https://hl7.org/fhir/R5/datatypes.html#Identifier) | `lossless` | `lossless` | `settled` | Direct equivalence. A FHIR `Identifier` with a `system` and **no** `value` has no `DV_IDENTIFIER` to become, because `id` is mandatory. |
| [DV_IDENTIFIER.issuer](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_identifier_class) `0..1` | [Identifier.system](https://hl7.org/fhir/R5/datatypes.html#Identifier) | `lossless` | `lossless` | `open` | **The `system` versus `issuer` mismatch — URI against free text — is the primary transformation challenge in this category.** Where the issuer is not a valid URI, the convention is to construct one by prefixing `http://openehr.org/identifier/`, and to strip that prefix again on the way back. Whether that prefix is a real definition or a placeholder, and whether a URN would be preferable to a URL, is **not decided**; guidance has been sought from HL7 TSMG. |
| [DV_IDENTIFIER.type](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_identifier_class) `0..1` | [Identifier.type](https://hl7.org/fhir/R5/datatypes.html#Identifier) | `lossless` | `lossy` | `open` | → openEHR: drops `Identifier.type` — openEHR carries the type as a single string in `system::value` notation, so only one coding survives — where several are present the `userSelected` one is taken, otherwise the first — and every other property of a `Coding`, including `version`, `display`, and `userSelected` itself, is dropped The `system::value` convention uses the **last** occurrence of `::` in the string, so no escaping is required even though `:` is valid in both URLs and codes — `http://[2001:db8::1]:8080/path::MR` parses correctly. Where no `::` is present, `http://openehr.org/identifier/type` is used as the system. |
| [DV_IDENTIFIER.assigner](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_identifier_class) `0..1` | [Identifier.assigner](https://hl7.org/fhir/R5/datatypes.html#Identifier) | `lossless` | `lossy` | `open` | → openEHR: drops `Identifier.assigner` — a FHIR `Reference` carries more than an identifier — `reference`, `display`, and `type` — and openEHR reduces the whole thing to one `system::value` string, so everything except the identifier is dropped On the way into openEHR the convention is to read `Identifier.assigner.identifier` and, where that is absent, the referenced `Organization.identifier`. |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_identifier_class)) | [Identifier.use](https://hl7.org/fhir/R5/datatypes.html#Identifier) | `unmapped` | `unmapped` | `open` | → FHIR: No `DV_IDENTIFIER` field produces it. (owner: `openehr-modelling`) → openEHR: Not carried at the data-type level. A **cluster archetype** providing `use`, `period`, and support for several types is drafted by the openEHR modelling team; this guide links it rather than authoring it. (owner: `openehr-modelling`) As a *consumer* of an identifier, `use` and `period` are rarely significant. They matter mainly for demographic entities, for temporary patient identifiers, and where the EHR is itself the assigner. |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_identifier_class)) | [Identifier.period](https://hl7.org/fhir/R5/datatypes.html#Identifier) | `unmapped` | `unmapped` | `open` | → FHIR: No `DV_IDENTIFIER` field produces it. (owner: `openehr-modelling`) → openEHR: Not carried at the data-type level; the extended cluster archetype provides it. (owner: `openehr-modelling`) Round-tripping therefore loses historic and superseded identifiers where they are distinguished only by their period. |
<!-- <<< mapping ledger: mapping:dv-identifier-to-identifier (generated by npm --prefix reference run render) <<< -->

#### Worked example - a medical record number

<!-- >>> mapping ledger: example:dv-identifier-to-identifier/01-medical-record-number.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_IDENTIFIER",
  "id": "12345",
  "issuer": "http://example.org/hospital",
  "type": "http://terminology.hl7.org/CodeSystem/v2-0203::MR",
  "assigner": "http://example.org/org::OrgRef123"
}
```
<!-- <<< mapping ledger: example:dv-identifier-to-identifier/01-medical-record-number.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-identifier-to-identifier/01-medical-record-number.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "use": "usual",
  "type": {
    "coding": [
      {
        "system": "http://terminology.hl7.org/CodeSystem/v2-0203",
        "code": "MR"
      }
    ]
  },
  "system": "http://example.org/hospital",
  "value": "12345",
  "period": {
    "start": "2020-01-01"
  },
  "assigner": {
    "identifier": {
      "system": "http://example.org/org",
      "value": "OrgRef123"
    }
  }
}
```
<!-- <<< mapping ledger: example:dv-identifier-to-identifier/01-medical-record-number.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_URI / DV_EHR_URI and uri / url

<!-- >>> mapping ledger: mapping:dv-uri-to-uri (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — DV_URI](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_uri_class) · [openEHR RM — DV_EHR_URI](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ehr_uri_class) · [FHIR R5 — uri](https://hl7.org/fhir/R5/datatypes.html#uri) · [FHIR R5 — url](https://hl7.org/fhir/R5/datatypes.html#url)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [DV_URI.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_uri_class) `1..1` | *when* the target element is typed `uri`, `oid`, `uuid`, or `canonical`: [uri](https://hl7.org/fhir/R5/datatypes.html#uri) <br/>*when* the target element is typed `url` and the value is a resolvable address: [url](https://hl7.org/fhir/R5/datatypes.html#url) | `lossless` | `lossless` | `open` | Direct equivalence — both are RFC 3986 URIs. **Which FHIR primitive is the target is decided by the element definition, not by the openEHR source type**: FHIR provides several URI primitives with progressively narrower constraints, and `canonical` additionally carries versioning semantics. This section has not been reviewed from either side. |
| [DV_EHR_URI.value](https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_ehr_uri_class) `1..1` | *when* the `ehr:` URI is carried opaquely, as an identifier of the source item: [uri](https://hl7.org/fhir/R5/datatypes.html#uri) <br/>*when* the URI is a `LINK.target` and is resolved to a FHIR resource: [Reference.reference](https://hl7.org/fhir/R5/references.html#Reference) | `lossless` | `lossy` | `open` | → openEHR: drops `Reference.reference` — the `ehr:` scheme addresses compositions, sections, entries, and sub-elements inside an openEHR EHR, and a FHIR reference addresses a resource; a value that did not originate as an `ehr:` URI cannot be turned into one, so only opaquely round-tripped values survive **No standard FHIR scheme conveys `ehr:` semantics.** Carrying the URI opaquely preserves it exactly but means nothing on the FHIR side can resolve it; resolving it to a `Reference` makes it usable but is a one-way transformation. |
<!-- <<< mapping ledger: mapping:dv-uri-to-uri (generated by npm --prefix reference run render) <<< -->

#### Worked example - an ehr: URI

<!-- >>> mapping ledger: example:dv-uri-to-uri/02-ehr-uri.openehr.json (generated by npm --prefix reference run render) >>> -->
```json
{
  "_type": "DV_EHR_URI",
  "value": "ehr://8f3c6b1e-1e5a-4a6b-9a3e-2b6f0c1d4e77/compositions/1"
}
```
<!-- <<< mapping ledger: example:dv-uri-to-uri/02-ehr-uri.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-uri-to-uri/02-ehr-uri.fhir.json (generated by npm --prefix reference run render) >>> -->
```json
"ehr://8f3c6b1e-1e5a-4a6b-9a3e-2b6f0c1d4e77/compositions/1"
```
<!-- <<< mapping ledger: example:dv-uri-to-uri/02-ehr-uri.fhir.json (generated by npm --prefix reference run render) <<< -->

### LINK and Reference / CodeableReference

<!-- >>> mapping ledger: mapping:link-to-reference (generated by npm --prefix reference run render) >>> -->
**Sources:** [openEHR RM — LINK](https://specifications.openehr.org/releases/RM/latest/common.html#_link_class) · [FHIR R5 — Reference](https://hl7.org/fhir/R5/references.html#Reference) · [FHIR R5 — CodeableReference](https://hl7.org/fhir/R5/datatypes.html#CodeableReference)

| openEHR field | FHIR target | → FHIR | → openEHR | Maturity | Notes |
|-|-|-|-|-|-|
| [LINK.target](https://specifications.openehr.org/releases/RM/latest/common.html#_link_class) `1..1` | [Reference.reference](https://hl7.org/fhir/R5/references.html#Reference) | `lossy` | `lossless` | `open` | → FHIR: drops `LINK.target` — a `LINK.target` can address a **sub-element** of a composition, and `Reference.reference` is a resource-level pointer whose `#fragment` is reserved for contained resources; sub-element granularity is lost unless the `targetElement` or `targetPath` extension is used Two standard extensions cover sub-element addressing when it is genuinely required: `targetElement`, carrying a target `Element.id`, and `targetPath`, carrying a restricted FHIRPath expression. Prefer `targetElement` where you control the target instance and can guarantee an `Element.id`; use `targetPath` where you cannot. **A mapping into FHIR SHOULD NOT produce either unless the source data genuinely needs sub-element granularity**, because most consumers do not interpret them. |
| [LINK.meaning](https://specifications.openehr.org/releases/RM/latest/common.html#_link_class) `1..1` | [Reference.display](https://hl7.org/fhir/R5/references.html#Reference) | `lossless` | `lossless` | `open` |  |
| [LINK.type](https://specifications.openehr.org/releases/RM/latest/common.html#_link_class) `1..1` | [CodeableReference.concept](https://hl7.org/fhir/R5/datatypes.html#CodeableReference) | `lossless` | `lossless` | `open` | The link type — `issue`, `problem`, `citation`, and the like — has no home on a plain `Reference`. A `CodeableReference` carries a concept **and** a reference in one value, which is the closest FHIR shape. |
| — <br/>*no counterpart* ([inventory](https://specifications.openehr.org/releases/RM/latest/common.html#_link_class)) | [CodeableReference](https://hl7.org/fhir/R5/datatypes.html#CodeableReference) | `unmapped` | `unmapped` | `open` | → FHIR: No single openEHR value produces a `CodeableReference`. (owner: `openehr-modelling`) → openEHR: The value must be **split** into a coded element plus a `LINK`, or into an enclosing cluster — for example an anatomical-location cluster alongside a coded body-site element. That is an archetype decision, not a data-type mapping. (owner: `openehr-modelling`) |
| [PARTY_IDENTIFIED](https://specifications.openehr.org/releases/RM/latest/common.html#_party_identified_class) `0..1` | — <br/>*no counterpart* ([inventory](https://hl7.org/fhir/R5/references.html#Reference)) | `unmapped` | `unmapped` | `open` | → FHIR: Not mapped here. openEHR splits internal record links (`LINK`) from external party references (`PARTY_IDENTIFIED`) and the working group is investigating unifying both onto a common Reference Model class, likely based on `OBJECT_REF`. **This guide maps the RM as published today and does not anticipate the unreleased change.** (owner: `openehr-modelling`) → openEHR: Whether an incoming `Reference` becomes a `LINK` or a `PARTY_IDENTIFIED` cannot be decided from the reference alone; it depends on what is referenced, which is archetype context. (owner: `openehr-modelling`) |
<!-- <<< mapping ledger: mapping:link-to-reference (generated by npm --prefix reference run render) <<< -->
