### Textual data

openEHR carries text in `DV_TEXT`, an attribute-rich class with `value`,
`formatting`, `encoding`, `language`, `mappings`, and the deprecated
`hyperlink`. FHIR uses three plain-text primitives — `string` (UTF-8, capped at
1 MiB), `markdown` (CommonMark), and `xhtml` (constrained XHTML) — plus
extensions that let a `string` element carry a richer rendered form alongside
its plain value.

This asymmetry is the single most important thing to understand about the
category: **a FHIR `string` is a value, and an openEHR `DV_TEXT` is a class.**
Everything a `DV_TEXT` carries beyond `value` needs somewhere else to go.

Five cross-cutting facts govern the rows.

- **Character encoding.** FHIR mandates UTF-8 throughout. openEHR permits
  several character sets through its `character_sets` code system. Non-UTF-8
  content must be converted before mapping, and the original encoding is not
  preserved on the FHIR side.
- **Empty and whitespace-only strings.** FHIR strips leading and trailing
  whitespace and does not permit an empty `string`. An empty *optional* openEHR
  text value is dropped; an empty *mandatory* one is an upstream data error, not
  a mapping problem.
- **Length.** FHIR `string` is capped at 1 MiB. openEHR sets no formal cap. In
  practice this is unlikely to be reached, and an oversize value is handled as
  an exception rather than truncated silently.
- **Language.** FHIR represents language at the resource level
  (`Resource.language`) and, at element level, through extensions. openEHR
  carries `DV_TEXT.language` directly on the value, so the two are at different
  levels of the model. The element-level `language` extension is a `code`
  **required**-bound to `all-languages`, so it carries the tag alone: the
  `CODE_PHRASE.terminology_id` that stated which scheme the tag belongs to is a
  named drop on the way out, and is derived from the extension's own binding on
  the way back.
- **Term mappings.** `DV_TEXT.mappings` is not covered here. It belongs to
  [Coded Data](mapping-coded.html), under `TERM_MAPPING`.

`DV_PARSABLE` — an encapsulated string plus a `formalism` — and the deprecated
`DV_PARAGRAPH` are also in this category. `DV_PARAGRAPH` has not been examined
by the working group, and this guide records that rather than inventing a
mapping for it.

See [Conventions](conventions.html) for how to read the tables.

### Summary

<!-- >>> mapping ledger: summary:textual (generated by npm --prefix reference run render) >>> -->

<table>
<thead>
<tr><th>openEHR type</th><th>FHIR type</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Scope</th></tr>
</thead>
<tbody>
<tr><td><code>DV_TEXT</code></td><td><code>string | markdown</code></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td><code>datatype</code></td></tr>
<tr><td><code>DV_PARAGRAPH</code></td><td><code>markdown | string</code></td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>not-discussed</code></td><td><code>datatype</code></td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: summary:textual (generated by npm --prefix reference run render) <<< -->

### DV_TEXT and string / markdown

<!-- >>> mapping ledger: mapping:dv-text-to-string (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_text_class">openEHR RM — DV_TEXT</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#string">FHIR R5 — string</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#markdown">FHIR R5 — markdown</a> · <a href="https://specifications.openehr.org/releases/TERM/latest/SupportTerminology.html#_languages">openEHR TERM — languages code set (External_id: ISO_639-1)</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_text_class">DV_TEXT.value</a> <code>1..1</code></td><td><em>when</em> the target element is typed <code>string</code>: <a href="https://hl7.org/fhir/R5/datatypes.html#string">string.value</a><br/><em>when</em> the target element is typed <code>markdown</code> and <code>formatting</code> says markdown: <a href="https://hl7.org/fhir/R5/datatypes.html#markdown">markdown.value</a></td><td><code>lossless</code></td><td><code>lossless</code></td><td><code>settled</code></td><td>A direct mapping; the target <strong>type</strong> is decided by the FHIR element definition, not by the data. FHIR strips leading and trailing whitespace and does not permit an empty <code>string</code>: an empty <em>optional</em> openEHR value is dropped, and an empty <em>mandatory</em> one is an upstream data error. FHIR caps <code>string</code> at 1 MiB; openEHR sets no cap, and an oversize value is an exception rather than something to truncate silently. The FHIR side is modelled here as the primitive together with its <code>_</code>-sibling — the <code>value</code> plus <code>extension</code> pair — because that is what carries the extensions the rows below use. The openEHR attribute is <strong>mandatory</strong> and the FHIR element is optional, so an incoming instance that omits it cannot be converted: the reference implementation produces <strong>nothing</strong> rather than inventing a value. See <a href="conventions.html#mandatory-attributes">the mandatory-attribute rule</a>.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_text_formatting">DV_TEXT.formatting</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-rendering-markdown.html">string.extension[rendering-markdown]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossy</code></td><td><code>lossless</code></td><td><code>open</code></td><td>→ FHIR: drops <code>DV_TEXT.formatting</code> — <code>markdown</code> is carried, by the <code>rendering-markdown</code> extension or by choosing a <code>markdown</code> target. What is dropped is the distinction between <code>plain</code> and <code>plain_no_newlines</code> — FHIR has no element recording it — and the legacy deprecated CSS string, which has no FHIR representation either Where <code>formatting</code> is <code>markdown</code>, the FHIR target SHOULD be a <code>markdown</code> element, or a <code>string</code> carrying the <code>rendering-markdown</code> extension, and a FHIR <code>string</code> carrying that extension on the way back sets <code>formatting</code> to <code>markdown</code>. <strong>There is no <code>html</code> formatting value.</strong> RM § 5.1.7 enumerates <code>formatting</code> exhaustively — <code>Void</code>, <code>"markdown"</code>, <code>"plain"</code>, <code>"plain_no_newlines"</code>, and a legacy deprecated CSS string — and rejects HTML as a formatting approach explicitly, so this guide recommends no XHTML target and emits none. XHTML support on the openEHR side is <strong>pending a Reference Model change request</strong>; until it lands the guide publishes it as a gap, on the row below and in the <a href="gaps.html">FHIR features with no openEHR counterpart</a> inventory.</td></tr>
<tr><td>—<br/><em>no counterpart</em> (<a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_formatting_and_hyperlinking">inventory</a>)</td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-rendering-xhtml.html">string.extension[rendering-xhtml]</a><sup>†</sup> <em>(extension)</em></td><td><code>unmapped</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ FHIR: Nothing produces one. No <code>DV_TEXT.formatting</code> value states XHTML rendering, so this guide emits no <code>rendering-xhtml</code> and recommends none. (owner: <code>openehr-modelling</code>) → openEHR: drops <code>string.extension[rendering-xhtml]</code> — a received <code>string</code> may state XHTML rendering and <code>DV_TEXT.formatting</code> has no value to record it, so the rendering instruction is not carried. <code>DV_TEXT.value</code> converts faithfully, so the text is kept and the instruction alone is the drop — refusing the whole conversion would discard content that maps perfectly well This is the sub-case that the <code>formatting</code> row above deliberately does not fold in: it has <strong>no openEHR endpoint at all</strong>, so it carries its own verdicts and its own citations rather than borrowing the parent row’s. It closes when the Reference Model change request adding XHTML support lands, at which point this becomes an ordinary <code>formatting</code> value.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_code_phrase_class">DV_TEXT.language</a> <code>0..1</code></td><td><a href="https://hl7.org/fhir/extensions/StructureDefinition-language.html">string.extension[language]</a><sup>†</sup> <em>(extension)</em></td><td><code>lossy</code></td><td><code>lossy</code></td><td><code>open</code></td><td>→ FHIR: drops <code>DV_TEXT.language.terminology_id</code> — the <code>language</code> extension declares <code>value[x]: code 1..1</code> with a <strong>required</strong> binding to <code>all-languages</code>, so it carries the tag alone; the <code>CODE_PHRASE.terminology_id</code> that stated which scheme the tag belongs to has no home on a bare <code>code</code> → openEHR: drops <code>string.extension[language][region-subtag]</code> — FHIR’s <code>all-languages</code> binding is <strong>BCP 47</strong> and openEHR binds <code>language</code> to its own published <code>ISO_639-1</code> code set. BCP 47 strictly contains ISO 639-1, so <code>en-US</code> and <code>zh-Hant</code> are BCP 47 tags that ISO 639-1 has no code for. The primary language subtag is carried and the region or script subtag is not; <code>string.extension[language][outside-code-set]</code> — a tag that is not an ISO 639-1 alpha-2 code — a three-letter ISO 639-2 code, a private-use tag — is outside the code set <code>Language_valid</code> requires, so <strong>no <code>language</code> is written at all</strong> rather than a <code>terminology_id</code> being invented for a code the code set does not hold. <code>language</code> is <code>0..1</code>, and the text itself converts faithfully The two standards carry language at <strong>different levels of the model</strong>. openEHR puts it on the value; FHIR puts the primary language on <code>Resource.language</code> and uses element-level extensions — <code>language</code>, <code>translation</code>, <code>additional-language</code>, and <code>narrativeLanguageControl</code> — for anything finer. Which of those applies is a resource-level decision, so only the element-level <code>language</code> extension is stated here. A cluster archetype for narrative, accounting for narrative language control, is owned by the openEHR modelling team. Coming back, the two standards do not bind language to the same set: FHIR’s <code>all-languages</code> binding is <strong>BCP 47</strong> and openEHR publishes exactly one language code set, <code>Id: languages, External_id: ISO_639-1</code>, which <code>DV_TEXT.Language_valid</code> requires the value to belong to. BCP 47 <strong>strictly contains</strong> ISO 639-1, so the inbound conversion is a narrowing and says so, rather than deriving an openEHR identifier from a FHIR binding.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_text_class">DV_TEXT.encoding</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#string">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: The character set is not carried. openEHR permits several through its <code>character_sets</code> code system; <strong>the sender is responsible for converting to UTF-8 before mapping</strong>, and the original encoding is not preserved. → openEHR: Nothing arrives. A receiving system MAY set <code>encoding</code> to UTF-8, which is an inference from the FHIR rule rather than a carried value.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_text_class">DV_TEXT.hyperlink</a> <code>0..1</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#string">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: <code>hyperlink</code> is <strong>deprecated</strong> in the openEHR Reference Model. Where one is present it MAY be folded into markdown content rather than dropped, which is a transformation of the value rather than a mapping of the field. → openEHR: Nothing arrives, and nothing should: the field is deprecated.</td></tr>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_dv_text_class">DV_TEXT.mappings</a> <code>0..*</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#CodeableConcept">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>settled</code></td><td>→ FHIR: Not carried by <em>this</em> mapping. A <code>DV_TEXT</code> with term mappings SHOULD be mapped as a <code>CodeableConcept</code> instead — see <a href="mapping-coded.html">Coded Data</a> — where each mapping becomes an additional coding. → openEHR: A plain <code>string</code> carries no codings, so nothing arrives.</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-text-to-string (generated by npm --prefix reference run render) <<< -->

#### Worked example - a plain note

<!-- >>> mapping ledger: example:dv-text-to-string/01-plain-note.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_TEXT",
  "value": "Patient reports intermittent headache."
}
```

<!-- <<< mapping ledger: example:dv-text-to-string/01-plain-note.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-text-to-string/01-plain-note.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "value": "Patient reports intermittent headache."
}
```

<!-- <<< mapping ledger: example:dv-text-to-string/01-plain-note.fhir.json (generated by npm --prefix reference run render) <<< -->

#### Worked example - markdown with a language

<!-- >>> mapping ledger: example:dv-text-to-string/02-markdown-with-language.openehr.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "_type": "DV_TEXT",
  "value": "**Allergy:** penicillin",
  "formatting": "markdown",
  "language": {
    "_type": "CODE_PHRASE",
    "terminology_id": {
      "value": "ISO_639-1"
    },
    "code_string": "en"
  }
}
```

<!-- <<< mapping ledger: example:dv-text-to-string/02-markdown-with-language.openehr.json (generated by npm --prefix reference run render) <<< -->

<!-- >>> mapping ledger: example:dv-text-to-string/02-markdown-with-language.fhir.json (generated by npm --prefix reference run render) >>> -->

```json
{
  "value": "**Allergy:** penicillin",
  "extension": [
    {
      "url": "http://hl7.org/fhir/StructureDefinition/rendering-markdown",
      "valueMarkdown": "**Allergy:** penicillin"
    },
    {
      "url": "http://hl7.org/fhir/StructureDefinition/language",
      "valueCode": "en"
    }
  ]
}
```

<!-- <<< mapping ledger: example:dv-text-to-string/02-markdown-with-language.fhir.json (generated by npm --prefix reference run render) <<< -->

### DV_PARAGRAPH and markdown / string

<!-- >>> mapping ledger: mapping:dv-paragraph-to-markdown (generated by npm --prefix reference run render) >>> -->

<p><strong>Sources:</strong> <a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_text_package">openEHR RM — Text package</a> · <a href="https://hl7.org/fhir/R5/datatypes.html#markdown">FHIR R5 — markdown</a></p>

<table>
<thead>
<tr><th>openEHR field</th><th>FHIR target</th><th>→ FHIR</th><th>→ openEHR</th><th>Maturity</th><th>Notes</th></tr>
</thead>
<tbody>
<tr><td><a href="https://specifications.openehr.org/releases/RM/latest/data_types.html#_text_package">DV_PARAGRAPH.items</a> <code>1..*</code></td><td>—<br/><em>no counterpart</em> (<a href="https://hl7.org/fhir/R5/datatypes.html#markdown">inventory</a>)</td><td><code>unmapped</code></td><td><code>unmapped</code></td><td><code>not-discussed</code></td><td>→ FHIR: <code>DV_PARAGRAPH</code> has <strong>not been discussed</strong>. It is deprecated in the Reference Model but may appear in older data and archetypes, so it is listed rather than omitted. No mapping is asserted, because none has been agreed. (owner: <code>working-group</code>) → openEHR: Nothing is mapped into a deprecated type that has not been discussed. (owner: <code>working-group</code>)</td></tr>
</tbody>
</table>

<!-- <<< mapping ledger: mapping:dv-paragraph-to-markdown (generated by npm --prefix reference run render) <<< -->
