{
  "directions": ["toOpenehr"],
  "reason": "openEHR restricts fractional seconds to three digits, so a DV_TIME cannot produce the nine-digit FHIR value. The fixture demonstrates the inbound truncation the dv-time.value row declares."
}
